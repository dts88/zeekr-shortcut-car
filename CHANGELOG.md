# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

每个版本的小节会被 CI 自动摘出来，作为该版本 GitHub Release 的说明正文，
所以这里写的内容就是用户在 Release 页面看到的内容。

## [Unreleased]

（暂无）

## [0.21.0-alpha] - 2026-09-01

### 设置改成两栏

左侧分区列表，右侧该分区的内容。车机内置屏 3200px 宽，
一列设置横铺过去右边两千多像素基本是空的，眼睛还得沿着很长的距离来回扫。

用的是 `SlidingPaneLayout` —— androidx 自己的 `PreferenceHeaderFragmentCompat`
内部也是它。宽屏并排、窄屏可滑动，两种情况都是白拿的。

**点子项时只换右栏**，左栏留着：进了二级界面还看得见自己在设置的哪一块。

分区名和顺序仍然只在 `preferences.xml` 里声明一次 —— 每个分区改成了嵌套的
`PreferenceScreen`，右栏按 key 取子树，左栏则把内容清空只留标题。
另写一份导航列表的话，加了新分区却忘了同步，左栏就会少一项。

### 权限设置改成 Preference

八项权限各一行，摘要直接显示授权状态；**已授权的那行不可点** ——
点了也只是又跳一次系统设置，给一个什么都不会发生的按钮不如不给。

回到这个界面时重新查一遍：授权是去系统设置里点的，回来时状态多半已经变了。

整个界面在代码里生成而不写 XML：每一项的状态是<b>查出来的</b>，不是存下来的设置值，
写死一份 XML 再逐个改标题摘要，等于把同一件事声明两遍。

ADB 一键授权和系统白名单那几个工具留在原来的界面里，从开发者选项进 ——
它们要往屏幕上滚动输出日志，那是控制台不是设置项。

### 删掉用不到的车型布局

`getCarModel()` 经 `SettingsRegistry` 净化，取值只可能是极氪两档或自定义。
布局选择器里那一长串银河 E5 / L6 / L7 / 星舰 / 手机分支判断的取值早就选不出来了。

删掉 10 个布局文件及其分支，连带清掉只存在于那些布局里的控件代码
（车辆控制按钮、前轮/后轮模式、E5 多按钮快捷键、四个摄像头开关）。

**净减约 4500 行。**

### 过渡动画用系统自带的

- 切分区：淡入淡出。横向滑动会读成「进入下一级」，而切分区是平级的
- 进二级界面：系统的「打开」过渡，返回时框架自动播反向动画

没有自定义动画曲线 —— 系统的这两个已经说明了层级关系，自己编一套只会和别处不一致。

## [0.20.0-alpha] - 2026-09-01

### 内置存储改为开发者模式专属

**正常模式下只录到外置存储**，车机内置闪存不再作为落盘位置。

理由是取舍，不是洁癖：行车记录是持续不断的写入，闪存写入寿命有限，
而车机存储坏了通常换不了。不熟悉软件的人会接受默认值、一路点确定 ——
这个代价不该由他们承担，所以它不该是一个弹窗能放行的选择。

具体：

- **存储位置**照样列出全部检测到的卷。「内置存储」仍然列出来（藏起来只会让人
  以为软件没这个能力），但标注**「仅限开发者模式」**，选中时会说明原因并拒绝。
- **中转写入**同样受限：它本质就是「先写内置再搬走」，也是往内置存储规律性写入。
- **没插 U 盘时不再偷偷降级**：以前会静默回退到内置存储继续录，
  现在会明确告诉你没检测到外置存储、不录，并提示插盘。

开发者选项打开后，以上限制全部解除。

### 关于页面：加了外置存储说明

推荐用法、已实测可用的介质，以及一条隐私提醒 ——
U 盘上的录像是不加密的，随时可以拔下来在别的设备上读。

### 超级后视镜启动延迟改为 2 秒

## [0.19.2-alpha] - 2026-09-01

### 修复：超级后视镜开着，但重开应用后不出现

开关状态存着「开」，可**没有任何地方在启动时把服务拉起来** ——
所以每次重开应用都得去设置里关一次再开一次，它才真的出现。

其他悬浮窗（画面悬浮窗、录制按钮、补盲）都在启动流程里恢复，唯独漏了它。现在补上了。

拉起来之前等了 1.5 秒：后视镜要绑相机，而那会儿相机还在开。

### 确认：超级后视镜默认是关的

查过了，`isRearViewEnabled()` 的默认值就是 `false`，不用改。

## [0.19.1-alpha] - 2026-08-30

### 修复：码率的「低/中/高」一直没生效

查证一个提问时发现的：这个下拉框**从来没有影响过录制**。

两处创建录制器的地方都写死了 `setQualityLevel(3)`（最高档），
而设置里的码率值只在「强制 H.264」时才会被读到 ——
正常的 HEVC 路径下走的是另一套公式，用的是那个写死的档位。

所以不管选低、中还是高，录出来都是同一个码率（约 7.7 Mbps）。

现在档位跟着设置走：

| 档位 | 码率 | 约合 |
|---|---|---|
| 低 | 3.8 Mbps | 28 MB/分钟 |
| 中 | 5.4 Mbps | 40 MB/分钟 |
| 高 | 7.7 Mbps | 58 MB/分钟 |

> ⚠️ **默认值的实际效果变了**：以前无论选什么都是 7.7 Mbps，
> 现在默认「中」是 5.4 Mbps —— 文件小了约三成，画质相应略降。
> 想要原来的画质，把码率调到「高」。

### 新增：录到内置存储前会提醒

手动开始录制时，如果录像实际会落在内置存储上，先弹一次确认。两种情况都算：

- 设置里选的就是内置存储
- 选了 U 盘但**当前没检测到**（会静默回退到内置，这种更值得提醒）

**自动录制不弹**（开机自启、回前台、远程唤醒）：那些场景没人在看屏幕，
一个等着点确定的弹窗会让记录仪根本不录 —— 那比写内置存储糟得多。
自动路径仍然用原有的 Toast 提示。

## [0.19.0-alpha] - 2026-08-30

### 修复：关掉鱼眼校正时，视野角度失效

那根滑块只喂给反投影，所以一关校正它就没反应了 ——
**一个拖得动却不起作用的控件，比没有这个控件更糟**。

现在它是超级后视镜的全局设置：开着校正时由反投影消化，关掉时由取景兑现
（收窄取景等效于放大），**四路都一样**。两种模式下拖动的手感一致。

换算和校正那条路同一套说法：180° 整幅，110° 约六成，90° 一半。
收窄不会改变画面比例 —— 比例锁死这条没有例外。

### 录像角标：加实时码率

规格行后面多了实时码率，**每秒更新一次**。

**不增加开销**：字节数在写进文件时本来就在手上，而角标位图本来就每秒重画一次
（秒数变了才重画）。只是把一个已有的数字接到一行已有的文字上。

显示的是**真正写进文件的**大小折算出来的，不是设置里的目标码率 ——
编码是可变码率，画面越复杂写得越多，目标值只是个上限。

### 录像角标：左上角标应用名与版本号

和右上角的日期时间对称。录出来的文件常常拿去当证据或者发给别人，
落上是哪个应用、哪个版本录的，回头出问题才对得上。

单独一块贴图而不是把右上角那块拉宽：两块在对角，一块横跨整幅会把字拉糊。
内容不变，初始化时画一次，之后不重画。

### 关于页面精简

原来来源说明和免责占了两屏，把这一页本该回答的问题挤没了。现在按
「这是什么 / 项目地址 / 请杯咖啡 / 开源与来源 / 安全须知」排，各一段。

- **项目地址**：github.com/dts88/zeekr-shortcut-car
- **请作者喝杯咖啡**：PayNow 到 9271 6786（完全自愿，不影响任何功能）

两处压缩了但没有删：GPL-3.0 的继承关系是许可证要求；
openavm-recorder 是「保留所有权利」的项目，「没有复制代码」这句必须留着。

## [0.18.0-alpha] - 2026-08-30

### 「分辨率与码率」并入设置

原来是一个单独的子界面（884 行 Java + 357 行布局）。现在直接是设置里的三项：
**录制分辨率**、**录制码率**、**视频流探测结果**。

分辨率选项要问过相机才知道，所以异步填；填好之前先显示当前值，不留空白下拉框。

**「亮度 / 降噪调节」**和它的**恢复默认**移到「高级」。

### 分辨率的取值规则单独提了出来

只列**每一路都支持**的尺寸，取交集而不是并集。

用并集的话，列表里会出现只有某一路才有的尺寸 —— 选中之后，
没有这个尺寸的那几路会悄悄退回最接近的一个，界面写的和实际录的就对不上了。
合成条带尺寸（1280×5140 之类）也排除：环视那一路固定用探测到的尺寸，这个设置管不到它。

这两条规则抽成了 `ResolutionOptions`，有单元测试钉着 ——
选错的表现是「录出来的分辨率和设置里写的不一样」，肉眼很难发现。

### 修正：录制界面的「前」不该改

上一版把「前」改成「环视」，改到了 `getDefaultCameraName`，
而**录制界面那四个标签也用同一个方法** —— 于是四宫格左上角也变成了「环视」，这是错的。
那里的四个标签指的是环视流拆出来的四个 lane，「前」就是前方。

改名挪到了 `getRecordingCameraDisplayName`：「录哪几路」和「四宫格里这一格叫什么」
是两个问题，答案也不一样。

### 删掉「使用说明」

那是上游 EVCam 的内容，讲的是这个应用里已经不存在的设置。
留着比没有更糟 —— 照着它调会调不出所写的效果。

## [0.17.1-alpha] - 2026-08-30

### 修复：内置存储选不了

确认框弹出来却没有确认键 —— 按钮其实在，只是文字和背景同色，看不见。

这个应用的主题下，原生 AlertDialog 必须显式带上 `AlertDialogTheme`
（项目里早有这个样式，注释就写着「确保文字可见」），我新加的那个框漏了。

### 存储位置：列出真实存在的卷

以前只有「U盘 / 内置」两项，插两个盘时没法指定是哪一个。

现在按实际探测结果逐个列出，每一项带**卷名 + 剩余/总容量**：

```
U盘 1 (A1B2-C3D4)（剩余 45.2 GB / 64.0 GB）
U盘 2 (E5F6-7890)（剩余 12.1 GB / 32.0 GB）
内置存储
```

选中某个盘会把它的根目录钉住，否则检测逻辑永远落到第一个盘上。

### 极氪车型下「前」改为「环视」

那一路不是前摄像头，是车机拼好的整条 360° 环视流。
叫「前」会让人以为还有另外三路可选。多路配置下另外两路相应叫「座舱1」「座舱2」。

### 新增：开发者选项

在**「关于本应用」里连点「安全须知」20 次**，输入密码后打开。

以下移入其中，平时不显示：

| 项目 | 为什么 |
|---|---|
| 视频流配置的「环视+座舱3路」「自定义」 | 没做完 |
| 权限设置 | 排查用 |
| 上传日志 | 排查用 |
| 补盲选项 | 未完成 |
| 超视模式 | 未完成 |

**不持久化**：只存在内存里，所以应用更新后失效、冷启动后失效
（从后台切回前台不算，那时进程没变）。用完不用记得关，重启自然就没了。

半成品混在正常选项里，选中之后出问题会让人以为是应用坏了 —— 所以默认藏起来。

> 当前值一定保留：万一已经停在某个隐藏选项上，把它从列表里抹掉会让下拉框空白。

## [0.17.0-alpha] - 2026-08-30

### 设置界面重建在 PreferenceScreen 上

原来是 **1970 行手写布局 + 2850 行接线代码**。每加一个设置都要重复写一遍
「卡片 + 标题 + 说明 + 控件 + 找 id + 读值 + 写值 + 联动显隐」，
而分类、摘要、启用依赖这些框架本来就提供。

现在是 **330 行 XML + 640 行接线**，净减约 3800 行。

### 最实在的收益：启用依赖不再靠手写

以前「息屏录制要先开启动自动录制」这类关系，是靠手写显隐逻辑维持的，
写漏一处就会出现「开关能点但不生效」。现在用 `android:dependency` 声明，框架负责置灰：

- 息屏录制 ← 启动自动录制
- 角标附带录制规格 ← 时间角标
- 后视镜的四项设置 ← 超级后视镜开关
- 悬浮窗大小/透明度 ← 悬浮窗开关
- 按钮大小/文字大小 ← 录制悬浮按钮
- 预览分辨率 ← 预览用低分辨率

### 取值仍然一律走 AppConfig

所有 Preference 都 `setPersistent(false)`：**它们不自己往 SharedPreferences 里写**，
读写全部经过 AppConfig。

这一条是刻意的。AppConfig 的 getter/setter 里带着夹取、默认值和联动，
而且它是这些设置在整个应用里唯一的读取入口。让 Preference 自己持久化就多了一条写入路径，
key 稍有出入就是「设置看着改了、实际没生效」—— 这类问题在这个项目里已经栽过几次。
所以 XML 里的 key 只是标识符，不是存储键。

**因此升级不会丢设置**：存的还是同一份 `app_config`，同样的键和值。

### 分类

返回录制界面 / 录制 / 存储 / 超级后视镜 / 悬浮窗 / 系统 / 高级 / 关于

「返回录制界面」单独放在最上面一条 —— 车机上没有系统返回键，
侧滑抽屉行车时也不好按。

### 搬运过程中修掉的两处

- **滑块范围是编的**：我一开始给悬浮窗大小写了 100–600，
  实际是 `FLOATING_SIZE_TINY`(32)–`FLOATING_SIZE_MAX`(160)；
  按钮大小实际 32–100，文字 8–24。现在直接引用 AppConfig 的常量。
  范围写错的表现是「选了一个随后被夹掉的值」，又是显示与实际不符那一类。
- **漏了实时推送**：改悬浮窗大小/透明度要通知正在显示的窗口，
  否则得关掉再开才看得到。

### 录制摄像头改成多选

用 `MultiSelectListPreference`，选项按**这台车实际有几路**生成，
不再固定列四个。至少要保留一路 —— 全不勾等于关掉录制，而关录制有它自己的开关。

### 几个弹窗是原样搬的

相机映射、使用说明、日志上传（含设备名输入与确认）这几段流程
从旧 Fragment 里**整段搬到了 `SettingsDialogs`**，只把 `getContext()` 和 `appConfig`
换成参数。没有重写：这些流程里的细节（覆盖项的清除时机、上次会话日志的判断）
重推一遍容易漏。

日志上传原来会把按钮置灰改文字作为「上传中」的反馈；
PreferenceScreen 里点完对话框就关了，没有那个按钮，改成用 Toast 报开始和结果。

### ⚠️ 这是目前改动面最大的一版

设置界面每一项都重新接了一遍线，而我没法在车上验证。
**请把每个设置都点一遍**，尤其是开关类的（看看改完是否真的生效），
以及录制摄像头、存储位置、相机映射、日志上传这几个带弹窗的。

设置值本身不会丢 —— 存储格式没变，真出问题回退到 0.16.0 也读得到。
