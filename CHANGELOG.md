# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

每个版本的小节会被 CI 自动摘出来，作为该版本 GitHub Release 的说明正文，
所以这里写的内容就是用户在 Release 页面看到的内容。

## [Unreleased]

（暂无）

## [0.12.0-alpha] - 2026-08-30

「视频回看」整个换掉了，和「连续回放」合并成一个界面。

### 为什么是换掉而不是修

旧的回看界面点一下就崩。它的结构是照着 E5 那种「每路一个文件」写的 ——
一个界面开到 **5 个播放器**，靠来回创建和切换来显示不同的路。
绿屏、马赛克、卡顿都出在这上面。

而环视录像落盘时已经拼成了一个 **2×2 网格文件**（2560×2560），四路都在同一个文件里。
所以「放大其中一路」只是**在同一个解码器的输出上换个取景** ——
不切文件、不新建播放器、不重新 seek。那 5 个播放器从一开始就是多余的。

修一个结构上多余的东西不划算，所以整个换成了已经验证稳定的时间轴播放器。

### 现在的「视频回看」

抽屉里只剩一个入口（「连续回放（实验）」那条去掉了，两条指向同一个界面没有意义）。

- **点画面放大一路，再点还原**。放大哪一格取决于你点的位置
- **倍速**：0.5x / 1.0x / 1.5x / 2.0x
- **长按左侧列表**：分享或删除这段录制（整组分段文件一起，只删其中一段会在时间轴上留个洞）
- 原有的连续播放、分段续播、时间轴拖动、日期分组列表都保留

倍速和取景在**每次换段时重新下发** —— 换段等于换了一次播放器状态，
不重新下发的话，连续播放会在每个分段边界上悄悄变回 1.0x。

### 顺带修正：画面不再被拉伸

以前没有任何地方处理宽高比，2560×2560 的方形录像被拉伸铺满一块宽视图。
现在按比例居中，留黑边。

> ⚠️ 这一条会改变四宫格的观感（画面变小、左右有黑边）。
> 如果你原来更喜欢铺满，告诉我改回去 —— 这是取舍，不是对错。

### 删掉的东西

按「没有用的，研究明白，然后删掉」的规矩清掉了两条**整条不可达**的旧链：

- `PlaybackFragment` → `VideoAdapter` → `VideoPlayerActivity`（视频）
- `PhotoPlaybackFragment` → `PhotoAdapter` → `PhotoViewerActivity`（图片，同样结构）
- 以及 `PhotoGroupAdapter`、`VideoGroupAdapter`（没有任何引用）

加上旧回看界面本身，共 **13 个文件、约 2900 行**。
`VideoPlayerActivity` 是最后一个还在用 VideoView 的文件 ——
它不需要被改造成新播放器，它需要被删掉。

### 文档

`docs/zeekr-platform-notes.md` 第 7 节那份待办清单**本身已经过期**了 ——
还挂着早就做完的「实车验证四宫格显示」「实车验证录制链路」之类。
重写为五个小节：已确认 / 已发版待确认 / 从未测试 / 待办 / 车辆信号。
车辆信号那节列出了**已排除的**与**仍可试的**，没有当成死路封掉。

## [0.11.0-alpha] - 2026-08-30

鱼眼校正做出来了 —— 而且没有上 OpenGL。

### 新增：后视镜鱼眼校正

设置 → **鱼眼校正**，开关一个，视野角度一个滑块（90°–140°，默认 110°）。
只改显示，录下来的原始画面不动。

### 原本卡在哪，怎么绕过去的

之前几版一直欠着这一项，原因写在这里：校正是**非线性**的，
一个 2D 矩阵表达不了一条曲线，标准做法是上着色器；
而这台车机上「GL 自建 SurfaceTexture 顶替相机生产者」是已知会崩的。
「要校正」和「不能上 GL」直接对撞。

绕法是**分片逼近**：把输出切成 10×10 的小格，每格四个角各算一次反投影，
再用 `setPolyToPoly` 把源四边形映射到目标格。
每一格内部是线性的，格子够密，拼起来就足够接近那条曲线。

画的仍然是原来那个 TextureView —— 相机的消费者始终只有它一个，
也就是这台车机上验证过的那个结构。没有新 Surface，没有位图回读，没有 GL 管线。

### 为什么参数是「视野角度」而不是畸变系数

用的是**等距鱼眼模型**：原图半径正比于射线角度。
输出画面的边缘正好对应偏离光轴 fov/2 的射线，
所以「110°」是字面意义上的视野 —— 能对着画面判断合不合适。

而 Brown-Conrady 那类模型要 k1/k2 系数，得拿棋盘格标定才有意义，
没法凭手感调。（项目里那个走 GL 的旧 `FisheyeCorrector` 用的正是 k1/k2，
它从来没有在这台车上跑过。）

### 采样夹在本路之内

合成流里四路画面上下紧挨着。反投影算出的采样点一旦越界，
取到的就是**隔壁摄像头的画面**。所以坐标先夹到 [0,1] 再换算到纹理 ——
最坏只是边缘被拉伸，不会串画面。这条有单元测试钉着。

### 来源说明

分片逼近这个思路、等距模型和参数范围，是读 openavm-recorder 公开源码后
学到的**事实**。该项目保留所有权利、未授予开源许可，因此只借鉴做法、不复制代码，
本项目的实现与 12 项单元测试均为独立编写。关于页面已列明。

值得一提的是：两边独立撞上同一堵 GL 墙，又用了同一种绕法 ——
这本身就说明这条路是对的。

## [0.10.1-alpha] - 2026-08-30

补上后视镜欠着的两项（鱼眼校正仍未做，见文末）。

### 新增：窗口尺寸可调，宽高分开

设置 → **后视镜窗口尺寸**，宽、高各一个滑块，松手即套用到正在显示的窗口。

**宽高分开而不是正方形**：后视镜天然是横向的，喂给它的取景也是该路画面里一条横向的带 ——
硬做成正方形要么上下留黑边，要么把两侧裁掉。默认 720×320。
双指捏合仍然按当前比例整体缩放（捏合是「放大缩小」，不该顺手改变形状）。

### 新增：取景调整模式（蒙版）

**长按后视镜**进入。此时：

- 显示**整路未裁切的画面**，蒙版框画在上面，框外压暗
- **拖动**移动框，**双指**缩放框
- **点一下**确认并退出，取景自动保存

压暗的是会被裁掉的部分，所以框里就是最终显示的内容 —— 不用想象，直接看到。

做成「模式」而不是再加一种手势：同一块小窗上已经有三段分区 + 双指缩放，
再塞第四种手势会互相打架。

### ⚠️ 鱼眼校正仍未做

原因不变：校正是非线性的，矩阵做不了，必须上着色器；而这台车机上
「GL 顶替相机生产者」是已知会崩的做法。现在裁切与取景这条安全路径已经完整，
下一步单独处理校正，出了问题也能分清是谁的错。

## [0.10.0-alpha] - 2026-08-30

### 换掉播放器：VideoView → 自己持有的 MediaPlayer + TextureView

回放的毛病改了四轮都没根治（0.7.4 / 0.7.6 / 0.7.9 / 0.8.1），共同原因是 `VideoView`：

| 现象 | VideoView 的原因 |
|------|------------------|
| 小窗发虚/透明，**放大一路就没了** | 它是 SurfaceView —— 在窗口上挖洞。网格里四个窗口就是四个洞，放大成一路才只剩一个 |
| 快速点击就绿屏/马赛克 | MediaPlayer 的生命周期藏在它内部，每次 `setVideoPath` 重建，外面无法保证上一个真的收干净了 |
| 拖动几次后卡顿 | 没法预加载，每次都是完整的 stop → create → prepare |

新的 `ManagedVideoPlayer` 用 `MediaPlayer` + `TextureView`：

- **TextureView 画在窗口里、不挖洞** —— 透明问题从结构上消失
- 播放器由我们建、我们放，**没准备好就拒绝 seek**（这正是把解码器搞坏的那一步）
- 每次打开带一个代次号，**过期回调直接丢弃**
- 打开时先彻底释放上一个，不让两个解码器抢同一个 surface

**跳转改用 `SEEK_CLOSEST_SYNC`**（只跳关键帧）。精确跳转要从前一个关键帧一路解到目标，
2560×2560 的帧解起来不便宜，连续拖动会累积成明显卡顿；回看行车记录仪落在最近的关键帧
完全够用。

两个界面都换了。连续回放只剩「时间轴上该放哪一段」这一件自己的事；视频回看的五个
VideoView 变成五个播放器，单路模式也不用再「先 seek 再 pause」去纠正
「某些设备 seek 后会自动播放」—— 那种事后纠正正是因为状态不在我们手里。

顺带删掉了 `mediaPlayers` 映射和手动摘回调的代码，它们存在的唯一理由就是
VideoView 把播放器藏了起来。

**未转换**：`VideoPlayerActivity`（单独的单视频界面）。它是另一条路径，不在这次报的问题里。

## [0.9.1-alpha] - 2026-08-29

### 修正

- **后视镜切到其他界面后卡住，切回来也不恢复**。日志把原因写得很清楚 ——
  打开诊断页的那一刻：

  ```
  Camera 2 released preview surface
  Camera 2 closed
  All cameras closed (repair suppressed)
  ```

  `MainActivity` 退到后台时会关掉所有相机，除非
  `BlindSpotService.hasActiveCameraWindows()` 说还有悬浮窗在用。
  **后视镜是个新窗口，那个判断根本不认识它**，于是相机在它脚下被抽走了。
  切回前台也没用：前台只为自己的预览重开相机，不会管一个它不知道的窗口。

  两处都补上了：退后台时把后视镜算进「还有窗口在用相机」；服务里再加一个 2 秒的
  看门狗，发现相机没了就重新绑定 —— 关相机的地方有好几处（录制、息屏……），
  不是每一处都会想到通知这里，与其逐个接通知，不如自己确认。

- **贴边隐藏一次都没触发过 —— 那个条件根本不可能成立**。拖动时 `clampX` 已经把 x
  限制在最多 `屏宽 - PEEK(72)`，也就是窗口右边缘最近只能到距屏幕边 72px 处；
  而贴边判定的阈值我写的是 48px。**72 > 48，永远进不了判定范围。**
  阈值改为 152px（PEEK + 80），现在拖到底就会吸附。

### 已查明：车辆权限彻底拿不到，包括 normal 级别

你按了「申请车辆权限」（日志 `request=4101, result=-1`），之后立刻采集的报告里，
**五项全部仍是「未授予」——包括 `CAR_POWERTRAIN`，它是 `normal` 级别。**

`normal` 权限本该在安装时自动授予、申请必定成功。它都拿不到，说明
**App Lab 容器根本不向里面的应用发放 `android.car.*` 权限**，与保护级别无关。

这条线到此为止：转向灯、车门、车速、电量、**档位**都读不到。
之前设想的「挂倒挡自动切后视」也不成立。

## [0.9.0-alpha] - 2026-08-29

### 新增：超级后视镜

把环视合成流里**后方那一路**（2×2 的右上格）单独放大成一个悬浮窗。
开关在 **设置 → 超级后视镜**。

**手势**（横向分三段）：

| 位置 | 操作 |
|------|------|
| 左 1/3、右 1/3 | 拖动移动窗口 |
| 中间 1/3 | 上下滑调整取景范围（窗口不动，左右方向不变） |
| 任意位置 | 双指捏合缩放窗口大小 |
| 拖到屏幕边缘 | 自动贴边，只露出一条边，可再拖回来 |

位置、大小、取景范围都会记住；「后视镜取景与位置」旁的重置按钮可恢复默认。

### 实现取舍

**没有新建 GL 管线，也没有改相机会话。** 这台车机有一条硬性平台事实
（`docs/zeekr-platform-notes.md` 2.1）：用 GL 自建 SurfaceTexture 顶替相机生产者会崩，
而「普通 TextureView + 父容器用矩阵重画」是验证可行的结构 —— 四宫格预览就是这么做的。

纯裁切是线性变换，「取哪一路」和「取景蒙版」两层可以相乘成一个矩形，
所以后视镜沿用同一条安全路径。相机绑定复用主屏悬浮窗那个已经在跑的附加输出槽位
（两者因此不同时存在 —— 它们本就是同一件事的两种形态）。

### ⚠️ 鱼眼校正这一版还没有

每一路都是鱼眼镜头拍的，畸变很大，本该先校正再取景。但**校正是非线性的，矩阵做不到，
必须上着色器** —— 而现有的 `FisheyeCorrector` 走的正是上面那条已知会崩的路，
且从未在这台车上跑过。

把两个不确定性绑在一起上车，出了问题会分不清是谁的错。所以先把纯裁切这条安全路径做通、
在车上验证，再单独处理校正。

### 测试

`RearViewGeometry` 20 条、`RearViewTouchModel` 14 条。取景框算错的表现是「画面偏了一点」，
手势换算错的表现是「拖起来发飘」—— 两者在车上都很难用肉眼判断，所以判据都放在了单元测试里。

## [0.8.6-alpha] - 2026-08-29

### 修正

- **切换视频时画面变透明（能看到应用背后的东西）**。这是 0.8.1 那个修复的副作用，
  我当时用「隐藏 VideoView」来挡住脏缓冲区。

  问题在于 **SurfaceView 是在窗口上挖了个洞**，它自己的画面在洞后面合成。
  把它隐藏，就只剩那个洞 —— 于是能看穿到应用背后。

  改成**盖住而不是隐藏**：在 VideoView 上方放一个不透明的黑色遮罩，它画在窗口里、
  正好盖住那个洞。surface 全程不动，既挡住了脏帧，也不会露出背景。
  顺带省掉了每次切换都拆建 surface 的开销。

### 新增

- **切换耗时统计**。日志里现在会打印两个数：

  ```
  切换耗时 · 准备完成: xxx ms     ← 开文件 + 解析 + 配置解码器
  切换耗时 · 出现第一帧: xxx ms   ← 上面这些 + seek + 解到第一张能显示的画面
  ```

  这一秒到底花在哪，三段的修法完全不同：如果主要在「准备」，就该在当前段播放时
  预加载下一段；如果主要在「seek 到出帧」，就该改成只跳关键帧。
  **优化错了地方要付出一次重写的代价却换不来什么**，所以先量。

### 说明：三路不会让这里变慢

**连续回放只播环视一路**（0.7.6 起按文件名槽位过滤，座舱两路不参与时间轴）。
所以再加两路摄像头，这个界面的切换耗时不变。

## [0.8.5-alpha] - 2026-08-29

### 移除：已删功能的残骸

留着没用的东西有实际代价，已经付过两次：**0.8.3 我还在给「旧帧率等级」的迁移逻辑
打补丁**，而那个等级的界面 0.6.1 就撤了；**`getRecordingConfig()` 里的无限递归
bug 能存活，正是因为没有任何调用者** —— 没人跑，就没人发现。

这次清掉：

| 残骸 | 依据 |
|------|------|
| **MJPEG 推流配置**：约 30 个方法 + 全部键 | 功能早已移除，补盲设置页自己写着「MJPEG 推流模块已移除，隐藏该入口」 |
| `fragment_mjpeg_stream_adjust.xml` | 没有任何 Java 代码引用这个布局 |
| 副屏设置里的 MJPEG 按钮 | `findViewById` 之后立刻 `setVisibility(GONE)` |
| **`SingleCamera` 的 `streamSurface` 整套**（3 字段 + 3 方法 + 4 处分支） | `setStreamSurface()` 无调用者，字段恒为 null |
| `AppConfig.getRecordingConfig()` | 无调用者；无限递归 bug 的所在 |

`streamSurface` 那一组值得单独说：它的 `streamSurface != null` 分支**就长在
`createCameraPreviewSession()` 里** —— 本机最容易出问题、我反复读过的那段代码。
它们读起来像是「可能还挂着第三个消费者」，每次推理都要先排除一遍。

`AppConfig` 从 4151 行降到 3947 行。

### 规则

已记入 `docs/zeekr-platform-notes.md` 4.12：**功能移除时，配置项、常量、挂载点一并删掉，
不要只藏入口。**判断方法就是 grep 有没有外部调用者 —— 「以后可能会用」不是保留理由，
git 历史里都在。

## [0.8.4-alpha] - 2026-08-29

### 移除

- **旧的「标准/低」帧率等级整体删除**。它的界面在 0.6.1 合并两个帧率控件时就撤掉了，
  此后只是为了「被迁移」而存在 —— 而那个迁移本身还需要一个「就近吸附到真实档位」的
  辅助函数，因为它算出来的值（硬件标准 25fps 时是 12）根本不在提供的档位里。
  为一个应用已经不再拥有的概念留这么多机械，不值得。

  **录制帧率现在就是你选的那个**：原始帧率，或某个具体数值。没有第二个因素。

  清掉的东西：`KEY_FRAMERATE_LEVEL`、`FRAMERATE_STANDARD` / `FRAMERATE_LOW`、
  `migrateLegacyFramerateLevel()`、`nearestOfferedFps()`，以及 `RecordingConfig`
  里那份平行声明。「帧率等级」这个词在代码里已无残留。

  仍存有 `framerate_level=low` 的老安装会直接采用当前的「录制帧率」（默认原始帧率）。
  那个键已经没有任何代码读取，留在存储里是惰性的；为了删它而保留一次性清理逻辑，
  恰恰是这次要移除的那类机械。

## [0.8.3-alpha] - 2026-08-29

### 重构：每个枚举设置只声明一遍

0.8.2 加的启动自检是在真正的问题上盖了一层网，而且有一点反而更糟：**合法取值又被
写了第四遍**。这个设置本来就已经声明三处 —— `AppConfig` getter 里的默认值、
`SettingsFragment` 里的选项数组、布局 XML 里的显示文字 —— 靠人工保持一致。
任何一处对不上，用户看到的就是一个设置、程序遵守的是另一个。

现在 `SettingSpec` 持有一个设置的**键名、合法取值、默认值、显示名**，
`SettingsRegistry` 声明那六个枚举项。其余全部从这里派生：下拉框的选项与顺序、
getter 的默认值、启动自检、显示名。**「声明了两遍」不再可能发生，因为只有一遍。**

两个性质从结构上堵死了原来的洞：

- getter 一律过 `sanitize()`，**不合法的值根本读不出来** —— 启动自检降级为顺手
  清理存储，不再是唯一防线
- `indexOf()` 找不到时返回 **-1 而不是 0**，「没匹配上」和「匹配到第一项」终于能区分开
  —— 界面显示「四宫格」而实际录出长条，正是这两者被混为一谈造成的

setter 走 `writeEnum()` 并拒绝不合法的值，坏数据连写都写不进去。

### 顺带修正

- **旧「低帧率」迁移会丢失**。它算出 `max(10, 标准帧率/2)`，硬件标准 25fps 时得到 12
  —— 而 12 不在提供的档位里，写进去读出来会被 sanitize 回 `auto`，这次迁移等于白做。
  现在就近吸附到真实存在的档位。
- **录制模式下拉框不再手工映射位置 0/1/2**。那是本文件里最后一处「调换选项顺序就会
  静默改变实际存储值」的地方。

### 测试

新增 14 条。其中一条我第一次写错了：它断言「默认值必须是第一项」——
对预览分辨率和码率等级都不成立，而且那是**旧代码**才需要的约定（因为找不到会落到 0），
新设计不需要。已改为断言真正重要的性质：全新安装时界面选中的就是配置返回的值，
默认值排第几都无所谓。

## [0.8.2-alpha] - 2026-08-29

### 修正

- **设置界面显示的值与实际生效的值可能对不上**（表现：录制画面排列显示「四宫格」，
  录出来却是原始长条；把选项切走再切回来就好了）。

  设置页每个下拉框都是这么读的：

  ```java
  int selectedIndex = 0;
  for (...) if (VALUES[i].equals(current)) { selectedIndex = i; break; }
  spinner.setSelection(selectedIndex);
  ```

  存的字符串一个都没匹配上时，**界面默默显示第 0 项，而配置里还是那个对不上的值** ——
  屏幕上写着一回事，程序按另一回事跑。切换选项之所以能「修好」，只是因为那一下终于
  写进了一个两边一致的值。**设置页有八个下拉框都是这个形状**，不是某一个的问题。

  与其在每个读取点各打一个补丁，改成让配置自己兜底：**启动时自检一次**，凡是取值不在
  合法集合内的枚举项，一律修回默认值并记日志。覆盖录制画面排列、录制帧率、
  预览分辨率、录制模式、码率等级。

  这样无论坏值是怎么来的 —— 写入被打断、版本之间合法取值变了、还是装了旧版本回去 ——
  界面都不会再显示一个程序不会遵守的设置。

- **不做四宫格重排时会说明原因**。以前设置写着四宫格却录出长条，日志里看不出是哪一步
  没成立；现在会明确记录是「设置为原始长条」还是「源尺寸不像合成条带」。

## [0.8.1-alpha] - 2026-08-29

### 修正

- **回放切换时的绿屏 / 马赛克：挡住脏缓冲区，而不是去修解码**。

  关键线索是「拿到电脑上播是顺滑的」——**文件本身没问题**；解码器也早已确认支持
  2560×2560、最大并发 16 路。所以绿屏和马赛克不是数据坏了，也不是解码器不够用，
  而是**把脏缓冲区显示出来了**：

  - **全绿** = 一块分配了却从没写过的 YUV420 缓冲区（Y=0, U=V=0）
  - **马赛克** = 上一个文件的残帧

  `VideoView` 的那块 SurfaceView 在多次 `setVideoURI` 之间是复用的。播放器被中途拆掉
  时，缓冲队列里可能还留着旧解码器分配、未填充的缓冲区，直接被合成器拿去显示了。
  这也解释了你观察到的「切换后有概率恢复」——**没有任何东西是坏掉的**，只是画面上
  一直挂着垃圾，等新帧盖上去才好。

  改法：**切换开始就把画面藏起来，等 `MEDIA_INFO_VIDEO_RENDERING_START`
  （新视频真的渲染出第一帧）再显示**。视频回看与连续回放都做了。

  带 1.5 秒超时兜底：这个事件并非所有 MediaPlayer 实现都会发，真不发的话画面就永远
  不显示了 —— 那比它要避免的脏帧更糟，所以超时后强制显示，最坏退回旧行为。

- **分辨率选项改为只列「每一路都声明过」的尺寸**（取交集，而非并集）。

  用并集时列表里会出现只有某一路才有的尺寸，选了之后其余几路会悄悄退回最接近的一个
  —— 界面写着一个值、实际用的是另一个。本机上有两项属于这种情况：

  | 尺寸 | 情况 |
  |---|---|
  | 1280×5140 | 只有相机 2（合成流）有；而环视已固定用自己的尺寸，此设置本来就管不到它 |
  | 1280×800 | 只有相机 0 有；相机 1 会退回 1280×720 |

  去掉后剩 8 项，每一项在所有摄像头上都真实存在。若原先存的值已不在列表中
  （比如 1280×5140），会落回「默认」并写回配置，避免界面与配置对不上。

## [0.8.0-alpha] - 2026-08-29

### 修正

- **三路配置的环视分辨率没有钉住，只是碰巧能用**。环视相机不能跟随全局「目标分辨率」——
  **四联合成的内容只在 1280×5140 下存在**，选别的尺寸拿到的是一张普通单画面图，
  而不是「同一画面换个清晰度」。

  相机 2 压根没声明 1280×800，所以按最近匹配会落到 1280×720：

  | 全局目标分辨率 | 环视相机实际拿到 | 结果 |
  |---|---|---|
  | 1280×5140 | 1280×5140 | 正常 |
  | 默认 1280×800 | 1280×720 | **合成内容丢失** |
  | 1920×1080 | 1920×1080 | **合成内容丢失** |

  单路配置一直用 `setPreferredSize()` 钉住这一路；**三路配置那行是我在 0.6.0 误删的**
  （当时错误地以为强制尺寸撑爆了 HAL 并发预算，0.7.0 已证明真因是
  `configuredCameraCount >= 4`）。所以三路能工作，只是因为你手动把目标分辨率设成了
  1280×5140 —— 这不该是它的依赖。

  现在前台与后台两条初始化路径都钉住环视，座舱两路仍按全局设置各自挑。
  画质页的说明文字也写清了这一点。

### 设置项作用范围审计

| 设置 | 作用范围 |
|------|----------|
| **录制帧率** | **全局**，一个值下发给所有相机 |
| **码率等级** | **全局等级**，绝对码率按每路自己的分辨率算（对的：4K 那路和 720p 那路不该同码率） |
| **目标分辨率** | **全局**，但**环视除外**（见上，硬件事实而非偏好） |
| 曝光/白平衡/色调/降噪/锐度 | 全局 |

**车型选择的影响范围**：只改变摄像头数量、布局、是否走四宫格拆分、预览变换分支，
以及切换时重置录制摄像头勾选。**不改变**帧率、码率、画质中的任何一项。
反向只有手动相机映射会覆盖自动槽位分配。

详见 `docs/zeekr-platform-notes.md` 的 4.10 节。

### 待办

- 「三路」与「自定义」将来收进开发者模式（关于页连点 + 口令），默认只暴露
  「极氪7X（环视合成流）」。已记入 4.11 节，本版未实现。

## [0.7.9-alpha] - 2026-08-29

### 修正

- **界面不可见时播放器只 pause、不释放 surface**。日志里成片的

  ```
  E/BufferQueueProducer: SurfaceView[...MainActivity]#8 dequeueBuffer,TIMED_OUT state=DEQUEUED
  ```

  就是这么来的。

  **先说清是哪个 surface**：`api:3` 是 `NATIVE_WINDOW_API_MEDIA`，producer 是
  MediaPlayer；而摄像头预览**根本没用 SurfaceView**（全程是 `AutoFitTextureView`）。
  所以这是**视频回看**那几个 `VideoView` —— 它作为 fragment 挂在 MainActivity 里，
  surface 才以 MainActivity 命名。

  `pause()` 不释放 surface。窗口退到后台后 SurfaceView 的 surface 会被销毁，
  而 MediaPlayer 仍持有它，于是不停去 dequeue 一个再也不会被消费的缓冲区 ——
  几十条 TIMED_OUT，解码器也被拖到坏状态。**这和「切几组之后画面变绿」是同一个根**：
  从后台回来时解码器已经不干净了。

  `onPause` 保持只 pause（弹个对话框不该把播放整个拆掉），
  真正释放挪到 `onStop` —— surface 就是在那时消失的。回到前台时按
  `currentGroup` 重新加载。

  连续回放（`TimelinePlayerActivity`）是同一个问题，一并处理：`onStop` 释放并记下
  时间轴位置，`onResume` 开回原处并保持暂停 —— 离开时未必想让它继续跑。

## [0.7.8-alpha] - 2026-08-29

### 变更

- **悬浮元素的默认位置改为实车调好的坐标**（2026-08-29 报告第 7 节，3200×2000 屏）：

  | | X | Y | 其他 |
  |---|---|---|---|
  | 录制悬浮按钮 | 2943 | 205 | 65dp / 14sp |
  | 悬浮按钮（打开应用） | 2890 | 33 | 85dp / 75% |

  存的是「参照屏幕 + 该屏上的像素」，用时按实际屏幕等比换算。在这台车上得到的就是
  上面的原值；换一块尺寸不同的屏会落在同样的相对位置，而不是像裸像素那样跑到画面外。
  另外统一夹在屏幕范围内。

  主屏悬浮窗没动 —— 它还是关着的、停在出厂的 100,100 / 480×320，
  这份报告里不包含对它的定位决定。

### 已查明（诊断报告分析）

- **回放问题与解码能力无关，此前的怀疑已排除**。录出来的其实不是 1280×5140：
  开 `grid2x2` 后四路重新拼成 2×2，落盘是 **2560×2560 HEVC**。
  而主解码器 `c2.qti.hevc.decoder` / `OMX.qcom.video.decoder.hevc` 上限是
  **8192×4320、最大并发 16 路**，2560×2560 完全支持。

  所以两个猜测都不成立：尺寸没超限；并发上限 16 也远大于视频回看同时播的 3 路，
  **「画面变绿是解码器实例耗尽」同样不成立**。回放问题就在播放器一侧
  （0.7.4 / 0.7.6 已修）。

- **车辆权限的保护级别读出来了，结果是分裂的**——比「全都拿不到」有用得多：

  | 权限 | 级别 | 结论 |
  |------|------|------|
  | `CAR_EXTERIOR_LIGHTS`（转向灯） | signature\|privileged | **拿不到** |
  | `CONTROL_CAR_DOORS`（车门） | signature\|privileged | **拿不到** |
  | `CAR_SPEED`（车速） | **dangerous** | **可申请** |
  | `CAR_ENERGY`（电量） | **dangerous** | **可申请** |
  | `CAR_POWERTRAIN`（档位/手刹/点火） | **normal** | **应自动授予** |

  转向灯联动确实做不了。但 `GEAR_SELECTION` 只需要 normal 级别的 `CAR_POWERTRAIN` ——
  **挂倒挡自动切后视画面是可行的方向**，不依赖转向灯。

  下一步：诊断页点「申请车辆权限」，然后重新采集报告对比。

## [0.7.7-alpha] - 2026-08-29

### 修正

- **角标的规格行第一段没有，从第二段才出现**。初始化顺序问题：

  `createEncoder()` 里算好 `encoderSpecLine` 后会调 `applyWatermarkInfoLine()`，
  但那一刻 `eglEncoder` 还是 `null` —— 它是在稍后 post 到编码线程的 runnable 里才
  new 出来的，所以那次调用直接空转返回了。

  而分段切换、录制恢复等路径再次调用 `createEncoder()` 时，`eglEncoder` 已经存在，
  于是规格行正常写入 —— 这就是「第一段只有日期时间，第二段开始才有分辨率帧率」。

  现在在 EGL 编码器创建完、`setWatermarkEnabled(true)` 之后再补一次，第一段就有了。

## [0.7.6-alpha] - 2026-08-29

### 修正

- **后座（座舱2）预览有画面但录不下来**。又是同一个 `>= 4` 判断，这次在
  `getEnabledRecordingCameras()` 里：

  ```java
  if (cameraCount >= 4) {          // 三路时 left 永远进不来
      if (isRecordingCameraEnabled("left")) enabled.add("left");
      if (isRecordingCameraEnabled("right")) enabled.add("right");
  }
  ```

  更糟的是设置页那个「录制摄像头」勾选框用的是 `>= 3` —— 所以界面上「左」是勾着的、
  看起来已启用，录制时却被这里悄悄丢掉。现在按槽位序号分开判断：
  back `>= 2`、left `>= 3`、right `>= 4`，与界面一致。

- **连续回放把环视和座舱视频接成了一条时间轴**。三路录制时同一分段会写出三个文件：

  ```
  20260829_100000_front.mp4   环视合成流
  20260829_100000_back.mp4    座舱 1
  20260829_100000_left.mp4    座舱 2
  ```

  它们的时间戳前缀完全相同，而扫描时把所有 mp4 都收了进来，于是被当成时间上前后
  相接的三段。**连续回放现在只看环视那一路**，座舱各路不参与时间轴
  （新增 `RecordingTimeline.isSlot()`，4 条单元测试）。总时长和总大小之前也是三路
  重复累加的，一并修正。

- **视频回看：切换几组之后画面变绿**。`stopAll()` 确实释放了播放器，但三个回调仍挂在
  VideoView 上，而它们闭包引用着 `preparedCount` / `totalVideos` / `duration`。
  下一组会把这些计数清零并重新注册回调 —— 上一组还在途中的 `onPrepared` 就落到新状态上，
  计数被多加一次，`checkAllPrepared()` 提前判定完成，于是对还没准备好的视图执行播放。
  现在拆除时一并摘掉回调。

- **修掉一处无限递归**：`AppConfig.getRecordingConfig()` 里匿名类的
  `getCarModel()` 写成了 `return getCarModel();`，调的是它自己 —— 一旦被调用就是
  `StackOverflowError`。改为 `AppConfig.this.getCarModel()`。
  （目前还没有调用者，属于未引爆的雷。）

### 新增

- **诊断报告 8.2 增加「最大并发解码实例」**（`getMaxSupportedInstances()`）。
  视频回看会把一组里的每一路**同时**解码，三路录制时就是 3 个解码器一起跑，
  其中一个还是 1280×5140。如果上限少于 3，同时播三路本身就超限 ——
  那才是画面变绿的根因，需要改成按需解码而不是一次打开整组。报告会直接标出来。

## [0.7.5-alpha] - 2026-08-29

> **三路配置在 0.7.4 实车验证通过** —— 三路画面全部正常显示。
> 根因确实是 0.7.0 修的那个 `configuredCameraCount >= 4` 判断，
> 跟分辨率、跟 HAL 并发能力都没有关系。

### 修正

- **座舱相机（标签「左」）在预览里躺着**。这一档以前没有自己的分支，掉进了 E5 的兜底
  逻辑 —— 那里的 `"left"` 指的是**装在车身左侧的摄像头**，要转 270° 才正。
  但在三路配置里，`texture_left` 只是第三个槽位，装的是一路普通座舱相机，本来就是正的。

  这个旋转只作用于预览（TextureView 的矩阵），拍照通道完全不经过它 ——
  所以现象才是「预览里躺着，拍出来的照片却是正的」。

  270° 加上你要的顺时针 90° 正好是 360°，也就是**不旋转**：
  结构上该怎么改，和实测要求的角度，指向的是同一个答案。

### 变更

- **三路布局重新分配宽度**。按 3200×1796 实测：

  | | 合成流 | 座舱列 | 按钮列 |
  |---|---|---|---|
  | 之前 | 1676px | 914px | 610px |
  | 现在 | 1676px | **1067px** | **457px** |

  按钮列实际只需要约 312px（两列 88dp + 边距），却分到了 610px；
  而座舱那两路 16:10 的画面在 914px 宽的格子里，纵向要浪费 311px。
  把多余的宽度让给座舱列后，浪费降到 215px。合成流那一列没动 ——
  它是正方形，1676 已经够用。

## [0.7.4-alpha] - 2026-08-29

### 修正

- **连续回放反复拖动后抽搐 / 卡死 / 乱码**。你的判断方向是对的——确实是播放器状态没管好，
  但不是「打开的文件越来越多」，而是**对一个还没准备好的播放器做了 seek**。

  `setVideoPath()` 只是发起**异步** prepare，而代码在发起的同时就把 `currentSegmentIndex`
  改成了新段号。于是在 prepare 完成之前再拖一次，如果落点还在同一段，
  就走进「同段直接 seek」分支，对一个尚未 prepare 的播放器调用 `seekTo()` ——
  解码器由此进入坏状态：先几帧几帧抽搐，然后卡住（再拖也不恢复），最后出乱码。
  **完全对应你描述的顺序：第一次拖没问题，之后越来越糟。**

  另外两个连带问题：

  - `mp.setOnSeekCompleteListener(m -> videoView.start())` 挂上后从不清除，
    导致该文件的**每一次** seek 都强制开始播放——包括你已经暂停之后。
  - `setOnPreparedListener` 每次切文件都重新注册一次，闭包捕获的是那一次的偏移量；
    切换密集时可能把旧偏移套到新打开的文件上。

  现在用 `preparingSegmentIndex` / `preparedSegmentIndex` 明确区分「正在打开」和
  「确实可用」，三个监听器只注册一次，未 prepare 完成时不碰播放器；拖动落在正在打开的
  同一段上时只更新目标偏移，不再重复打开。另外加了连续出错计数，避免「出错→跳下一段→
  又出错」一路翻到底。

### 新增

- **诊断报告第 8 节：回放与解码能力**。这是上面那个修复<b>无法覆盖</b>的另一种可能，
  需要实测数据才能排除：

  - **8.1** 用 `MediaExtractor` 读最近 3 个录像文件的**真实**规格（编码、尺寸、时长）
  - **8.2** 枚举本机 AVC/HEVC 解码器声明的宽高上限，并**直接询问它支不支持这些尺寸**
    （`VideoCapabilities.isSizeSupported`）

  为什么重要：合成流是 **1280×5140**，而多数 AVC 硬件解码器单边上限是 **4096**。
  如果超了，播放会退回软解（卡顿抽搐）或直接解错（乱码）——**那就不是播放器代码能修的**，
  得改录制尺寸或分格保存。报告会直接给出「支持 / >> 不支持 <<」的结论。

## [0.7.3-alpha] - 2026-08-29

### 变更

- **「申请车辆权限」按钮现在申请全部尚未授予的车辆权限**，不再只挑 dangerous 的。
  「车机会不会弹授权框」本身就是要在车上观察的实验，替系统先筛掉一部分就把实验
  做没了。dangerous 的会弹框、signature/privileged 的系统当场静默拒绝 —— 两种结果
  用户都能直接看到，申请完自动重采报告对照。

## [0.7.2-alpha] - 2026-08-29

### 修正上一版的结论

上一版说「这些权限是 signature|privileged，第三方应用拿不到」——**那是我凭对 AOSP
的印象说的，不是这台车机给出的答案**，而各 Android 版本、各家 OEM 的定义并不一致。

更要紧的是：那份报告是在**我们一个车辆权限都没声明**的时候采集的。
`checkSelfPermission` 对未声明的权限同样返回 DENIED，所以当时的「未授予」和
`getPropertyList()` 返回 0，**只能说明我们没问过，不能说明会被拒**。

### 新增

- **报告打印每个车辆权限的保护级别**（`PermissionInfo.getProtection()`）。
  这是能不能拿到的决定性依据，而且是这台车机自己的答案：

  | 级别 | 含义 |
  |------|------|
  | `dangerous` | 运行时可申请，值得弹窗试一次 |
  | `signature` / `privileged` | 需与平台同签名或预置到 priv-app，App Lab 里拿不到 |
  | 未定义 | 本平台根本没有这个权限 |

- **诊断页新增「申请车辆权限」按钮**。声明一个 dangerous 权限而不申请，它依然是
  拒绝状态 —— 所以光靠 0.7.1 的声明并不足以改变结果。这个按钮只申请本机报告为
  dangerous 的那些，申请完自动重新采集报告；一个都没有时会直接说明，
  而不是发一个系统会静默丢弃的请求。

采集建议：进诊断页 → 点「申请车辆权限」→ 看弹窗（如果有）→ 报告会自动刷新。
这样下一份报告里的「未授予」才是真结论。

## [0.7.1-alpha] - 2026-08-29

本版基于 0.7.0 的实机诊断报告，主要是修正报告本身的错误结论，并把已经问明白的
事情记录下来。功能改动很少。

### 修正

- **「一次只保证打开一路相机」这句话是错的**。`getConcurrentCameraIds()` 返回空，
  只说明 HAL 没有**保证**任何组合，**不等于不允许并发** —— 自定义配置下三路画面
  确实能同时显示。报告里那句结论已改掉，后面基于它的排查提示也一并改了。

- **`[小窗（补盲/画中画）]` 是个错名字**。那组配置驱动的是 `FloatingWindowService`，
  它画的是 `FloatingButtonView` —— 一个点击打开应用、随录制状态变色的**悬浮按钮**，
  跟画中画没关系。已改为「悬浮按钮（打开应用/状态指示）」。

  另外两个名字是对的，但三个浮动元素放在一起容易混，所以都加了括号说明：
  「主屏悬浮窗（摄像头画面窗口）」「录制悬浮按钮（开始/停止录制）」。

- **报告里第 7、8 节的编号是反的**（悬浮窗那节标着 8 却排在 7 前面）。已对调。

- **屏幕尺寸只报了应用可用区域**（3200×1796，不含系统栏），但悬浮窗用的是整屏坐标
  （3200×2000）。只看前者会把位置算错一整条系统栏的高度。现在两个都报。

### 新增

- **声明了 5 个 `android.car.permission.*` 权限**，并让报告分别显示
  「已声明 / 已授予」两栏。

  原因见下 —— 之前的报告只显示「未授予」，而 `checkSelfPermission` 对
  「没声明」和「声明了但被拒」都返回 DENIED，不声明就分不清是哪一种。

- **报告会打印每个车辆相关 binder 服务的 AIDL 接口描述符**。接口名比服务名更有
  指向性，是继续往下找的线索。

### 已查明：转向灯 / 车门信号读不到，卡在权限而不是 API

诊断报告给出了明确结论，两个猜测都被推翻：

| 猜测 | 实测 |
|------|------|
| 按类名找 `CarSignalManager` | 全不存在（本来也不是 EVCam 的读法） |
| ECARX binder 链（`ecarxcar_service` → `ecarx.car.ECarXCar`） | **服务和类都不存在**，极氪 7X 不走 ECARX 那套 |

真正可用的是**标准 Android Automotive**，而且每一步都成功：

```
android.car.Car                 存在
Car.createCar(context)          成功
getCarManager("property")       成功
读 TURN_SIGNAL_STATE 等          → SecurityException: requires android.car.permission.XXX
```

**API 正常，被权限挡住。** 这些权限是 `signature|privileged` 级别，需要与平台同签名
或预置到 `/system/priv-app` —— App Lab 里的第三方应用两个条件都不满足。

**对补盲模块的影响**：联动触发在极氪 7X 上做不了，只剩手动触发可用。
logcat 也扫了 4855 行、0 命中，日志里没有可识别的信号事件。

### 已查明：车机暴露了三块屏

| id | 类型 | 分辨率 | 状态 |
|----|------|--------|------|
| 0 | 内置屏幕 | 3200×2000 | ON |
| 2 | HDMI 屏幕 | 2560×536 | ON |
| 3 | HDMI 屏幕 | 1440×480 | ON |

两块副屏都是 ON，是真实在用的显示设备。2560×536 这个约 4.8:1 的细长比例，
正好适合放一条横向拼接的环视带 —— 副屏推送值得做，已记进待办。

### 已查明：真实平台身份

容器伪装了 `Build` 和 `ro.product.*`（自称 Pixel 3a / sargo），
但 **`ro.build.display.id` 没被改**：

```
zeekr_dhu_sa8295_ovs-userdebug 12 SQ3A.220705.003.A1 ...
ro.board.platform = msmnile
```

真实平台是**极氪 DHU，高通 SA8295，Android 12**，userdebug 构建。
识别车机应该读这一项而不是 `Build.MODEL`。

## [0.7.0-alpha] - 2026-08-29

### 修正

- **三路模式全黑：找到真正的原因了，与分辨率无关**。0.6.0 我猜是强制
  1280×5140 撑爆了 HAL 的并发预算 —— 猜错了，那段代码根本没被执行到。

  真正的原因在 `MainActivity`：

  ```java
  if (textureLeft != null && configuredCameraCount >= 4) {
      textureLeft.setSurfaceTextureListener(buildSurfaceListener("left"));
  }
  ```

  三路配置的 `configuredCameraCount` 是 3，所以 **texture_left 从来没拿到监听器**。
  于是 `textureReadyCount` 最多只能到 2，而 `requiredTextureCount` 是 3，
  启动相机的那道闸门

  ```java
  if (textureReadyCount >= requiredTextureCount && checkPermissions())
  ```

  **永远不成立 —— `initCamera()` 一次都没被调用过**。一路相机都没打开，
  所以不是「座舱两路黑」，而是三个画面一起黑，连合成流也没有。

  上游只有 1/2/4 路的布局，左右天然成对，把它们一起判断 `>= 4` 从没出过问题。
  「环视+座舱3路」是第一个 3 路配置，而它把 left 当作第三个槽位用。
  三个判断现在改成各按自己的序号：back `>= 2`、left `>= 3`、right `>= 4`。

  同一处偏差还影响标签显示和自定义布局的容器隐藏（3 路时会连 left 一起藏掉），
  一并改了。

- **录制帧率现在是唯一的设置**。`getActualFrameRate()` 只读 `record_fps` 一个值，
  旧的「标准/低」等级已彻底移除（连 getter/setter 一起删掉，免得以后有人再调它
  却发现没反应）。老版本升上来若停在「低」，第一次读取时自动折算成具体帧率，
  之后不再有第二个因素参与。

### 新增

- **视频角标可以附带录制规格**。原来只有一行时间戳，且除了开关没有任何选项。
  现在时间下面可以再加一行：`1280x5140  10fps  H.264  12.0Mbps`。

  写的是**编码器实际使用的值**，不是设置里的目标值 —— 请求的尺寸可能被夹过、
  编码可能回退到 H.264、帧率也可能被补盲模式改写。角标要如实反映录出来的东西。
  开关在「设置 → 角标附带录制规格」，需先开启「时间角标」。

- **连续回放左栏改为倒序 + 日期分组**。最新的在最上面，按日期分组显示
  「8月29日 星期六」这样的标题。日期已在标题里，条目本身就突出时间跨度
  （`12:54 – 13:21`），下面一行是时长、大小、段数。

  `RecordingTimeline.build` 仍然是正序 —— 时间轴偏移量依赖那个顺序，也有测试钉着；
  倒序纯粹是显示层的事，放在适配器里做。

### 变更

- 悬浮窗默认按钮大小 64dp → **85dp**，默认不透明度 100% → **75%**。
  （已经手动调过的不受影响；「重置为默认」会回到新值。）

### 说明：「录制状态显示」是做什么的

它控制**录制时**界面右上角那个 `00:00 / 1` 浮层 —— 已录时长和当前分段号，
**双击该浮层可以快速开关**。它只影响应用界面，**不写进录制出来的视频**；
不录制时也不显示，所以平时看不到是正常的。想让信息进到视频里，用的是
「时间角标」，这一版刚给它加了规格行。

## [0.6.1-alpha] - 2026-08-29

### 新增

- **连续回放加了左栏列表**。版式与视频回看一致：列表在左、播放器在右。
  一项是**一条连续时间轴**（不是一个文件），显示日期、起止时刻、总时长、
  总大小、段数；顶部一行汇总共几条、合计多长多大。当前播放的那条左侧有红色标记，
  用「上一段/下一段」切换时列表会跟着滚动并高亮。

  为此把文件大小接进了时间轴模型（`Source`/`Segment`/`Session.totalSizeBytes`）。
  三参 `Source` 构造保留，不知道大小的调用方不必编一个数字出来；读不到大小时
  列表显示「大小未知」而不是 0 B —— 后者看起来像个真实的测量结果。

### 修正

- **画质设置里「录制帧率」什么也不显示 —— 这是上一版我的疏漏**。合并两个帧率控件时
  我删掉了 `initFramerateSpinner()`，而它是 `updateFramerateDescription()` 唯一的调用者，
  于是那行字从来没被填过。

  按你的判断整块删掉，而不是补上：同一页的**当前录制参数**本来就报告帧率。
  顺带修了那里的一个真错误 —— 它显示的是旧的「标准/低」等级，而合并之后该等级恒为
  「标准」，与实际设置完全脱节。现在显示的是真正选中的档位与实际帧率。

- **悬浮窗布局的「重置为默认」挪到右侧**，与设置页其它带控件的行一致，不再独占一行。

- **悬浮窗「按钮大小」由十档下拉框改为滑块**，与录制悬浮按钮完全一致（小 ←→ 大 +
  dp 读数）。原来是超小/特小/小/中/大/超大/特大/特特大/PLUS大/MAX大 十个命名档位 ——
  一个数字配十个名字，而名字并不比数字多说明什么。取值范围不变（32–160dp），
  松手时才落盘。

- 侧边菜单去掉「基于 EVCam (GPL-3.0)」那一行（完整致谢仍在「关于与致谢」页）。

### 内部

- 时长/大小的格式化抽到 `TimelineFormat`（不依赖 Android API），
  播放器与列表共用一份，也才能写单元测试 —— 放在继承 `RecyclerView.Adapter` 的类里
  会把一整套 UI 类拖进纯 JVM 测试。新增 4 条测试，时间轴测试共 15 条。

## [0.6.0-alpha] - 2026-08-29

### 修正

- **「环视+座舱3路」重写，不再强制环视走 1280×5140**。这一档之前三个画面全黑，
  连合成流都看不到。对比一下就能看出可疑的是哪一项：

  | 配置 | 相机数 | 是否强制 1280×5140 | 结果 |
  |------|--------|--------------------|------|
  | 环视合成流（单路） | 1 | 是 | 正常 |
  | 自定义 | 3 | 否 | 三路都能看到 |
  | 环视+座舱3路 | 3 | **是** | **全黑** |

  强制分辨率正是两个三路配置之间唯一的差别。单路时只有一路相机，6.6MP 随便用；
  三路同开时，这一路再加两路座舱很可能超出 HAL 的并发流预算，而**会话配置失败是
  整组一起失败** —— 表现就是三个画面一起黑，包括合成流自己。

  现在这一档不再指定尺寸，各路自己协商（默认目标 1280×800，负担轻得多）。
  至于还要不要拆四宫格，交给实际协商到的尺寸决定：`FourLaneContainer` 会忽略任何
  不像合成条带的尺寸，于是直接显示原始画面，而不是把一张普通图切成四块错的。

  > 这仍是**推断**，不是实锤。装上后如果三路能出画面，就验证了；如果仍然黑，
  > 画面上会显示槽位分配（环视/座舱1/座舱2 各自用的是哪个相机 id），请连同
  > 诊断报告一起发回。

- **槽位分配抽成一份共享逻辑**（`ZeekrMultiPlan`，9 条单元测试）。前台和后台
  以前各写了一份，规则还不一致 —— **后台那份根本不看手动指定的相机映射**，
  于是同一台车前后台分出来的槽位可能不同。

- **侧边菜单的信息是错的**。此前仍显示「EVCam / 电车记录仪 / 作者：@greenteacher46
  （四十六）」，图标也一直沿用 EVCam 的。已改为「极氪即刻 / 车机版环视记录仪 /
  作者：dts88」，并加一行「基于 EVCam (GPL-3.0)　详见「关于与致谢」」。

  GPL 管的是代码，不含商标与图标 —— 把别人的名称、署名和 logo 当作本应用的身份
  标识本身就不合适，与许可证无关。

### 新增

- **恢复「自定义（排查用）」配置**。它不做任何极氪专属处理（不探测合成流、
  不强制分辨率、不走四宫格容器），单纯按数量把相机铺开。三路黑屏时，用它可以
  区分「相机开不起来」和「极氪那套处理有问题」。选中时会显示摄像头数量/布局配置按钮。

- **诊断报告新增第 8 节：悬浮窗位置与大小**。用途是把手动调好的位置定为默认值 ——
  在车机上把各悬浮元素拖到合适位置和大小，导出报告，把这一节发回即可写进代码。

  同时列出**当前值**与**现行默认值**（只有能看出差别才知道该改哪些），
  外加屏幕尺寸与密度（位置是像素值，换算要有依据）。覆盖主屏悬浮窗、
  录制悬浮按钮、小窗三组。

### 图标

- 新增本应用自己的图标（四宫格 + 录制点，纯矢量），用于启动图标与侧边菜单。
  移除不再引用的 `nav_logo.png`（EVCam 的 logo）。

## [0.5.2-alpha] - 2026-08-29

### 修正

- **回放速度不对，这次修的是病根**。0.4.3 那次只把写死的 25fps 步长改成跟随
  设置，属于治标 —— 病根是**时间戳按帧数推算**这件事本身。

  推算的写法是「第 N 帧的时间戳 = N × 标称步长」，它假设编码器真的按标称帧率
  收到了帧。而 1280×5140 的合成流跑不满 30fps：N 帧被标成 N/30 秒、实际却花了
  N/15 秒，回放就快了一倍。「原始帧率」这一档最明显，因为它的标称值最高。

  其实真实的采集时间一路都在，只是最后被丢掉了：

  ```
  surfaceTexture.getTimestamp()        真实采集时间
    → 以首帧为基准归一
    → eglPresentationTimeANDROID(...)  EglSurfaceEncoder.drawFrame
    → bufferInfo.presentationTimeUs    编码器输出
    → 被覆盖成 帧号 × 标称步长          ← 问题在这里
  ```

  现在直接采用编码器给出的时间戳。当初保留推算是担心抖动或丢帧导致时间戳乱序
  被 muxer 拒绝 —— 那个顾虑加一个单调性兜底就够了，不必牺牲真实时间。

  连带处理：分段录制时每段都是新的 muxer，PTS 需要归零。而
  `firstFrameTimestampNs` 整场录制不重置（EGL 要求单调递增），所以编码器时间戳
  会一路累加 —— 按每段首帧再减一次基准。推算法从 0 重新计数，所以以前没暴露这个问题。

  录制日志里每 300 帧会输出一次**实测帧率**，可用来核对：若它明显低于所选帧率，
  说明车机确实跑不满，而现在时间戳如实反映了这一点，回放不会再被加速。

- **两个「录制帧率」合并为一个**。设置页有一个（原始帧率/30/24/20/15/10），
  画质设置页还有一个（标准/低）。

  它们从来就不是两个独立设置：`getActualFrameRate()` 先看设置页选的具体帧率，
  **只有那里选「原始帧率」时才轮得到标准/低**。也就是说两个同名控件里有一个在
  悄悄压过另一个 —— 这正是「选了帧率却好像没生效」的另一个来源。

  现在只保留**设置页**那一个。画质设置页改为显示当前生效值（该页的码率估算仍要
  用到帧率），并注明去哪里修改。另外，选择帧率时会把旧的「标准/低」等级一并清回
  标准，否则选了「原始帧率」还会被旧等级悄悄减半。

### 新增

- **预览分辨率选项**（设置 → 预览用低分辨率 下方）。可选 640x480 / 1280x720 /
  1600x900 / 1920x1080。

  之前这里只有一个开关：配置项和读取侧（`SingleCamera` 按设定值在车机**已声明**的
  尺寸里挑最接近的）早就写好了，但界面一直没做，所以实际永远停在默认的 1280x720。
  现在补上了。未开启「预览用低分辨率」时该选项置灰 —— 那时预览与录制同分辨率，
  选什么都没有意义。

## [0.5.1-alpha] - 2026-08-28

这一版只做一件事：**把车辆信号探测重写**。装上后进「诊断」导出报告即可，
录制、回放等功能没有改动。

### 修正

- **之前的车辆信号探测结论是错的**。旧版按几个猜出来的类名去找
  `CarSignalManager`（`com.gwm.*`、`com.geely.*`、`com.zeekr.*` 等），
  一个都没找到，就报告「读不到车辆信号」。

  但这根本不是 EVCam 读信号的方式。读了它的实现才发现真正的路径是走
  binder 服务反射：

  ```
  ServiceManager.getService("ecarxcar_service")
    → ecarx.car.IECarXCar$Stub.asInterface(binder)
    → ecarx.car.ECarXCar.createCar(context, iECarXCar)
    → car.getCarManager("car_signal", iECarXCar)
    → getIndcrSts() / getDoorDrvrSts() / getDoorPassSts() / ...
  ```

  ECARX 正是做极氪车机的那家（EVCC 全称就是 Ecarx Vehicle Control
  Console），所以同一条路在极氪上很可能是通的 —— 之前那个「读不到」的
  结论下得太早了。

### 新增

诊断报告的第 4 节重写为六个小节，**每一项都独立 try/catch，一条路失败不会
中断整份报告**：

- **4.1 系统属性**。App Lab 的容器伪装了 `android.os.Build`（车机自称是
  Pixel 3a），但通常没伪装 `SystemProperties`。直接读 `ro.product.*`、
  `ro.board.platform`、`ro.ecarx.*`，看这台车机真正是谁家的平台 ——
  这决定了该找哪家的车辆 SDK。
- **4.2 系统服务清单**。`ServiceManager.listServices()` 列出全部 binder
  服务，筛出含 car / ecarx / vehicle / signal / vhal / hvac 的那些。
  **这是最有价值的一节**：与其继续猜名字，不如直接看车机注册了什么。
- **4.3 ECARX ecarxcar_service**。完整走一遍上面那条调用链，每一步的成败
  都写进报告，最后读转向灯与四个车门的状态；并把该 manager 上**所有无参
  getter 连同返回值**都列出来 —— 可能有我们还不知道的信号。
- **4.4 ECARX adaptapi CarSensor**。EVCam 里出现过的另一条路
  （`com.ecarx.xui.adaptapi.car.sensor.CarSensor`），同样列出全部 getter。
- **4.5 标准 android.car**。上一份报告已确认 `android.car` 存在，这次往里
  走：连 `Car` → 拿 `CarPropertyManager` → **列出本应用实际能访问的属性**，
  再逐个读转向灯 / 车门 / 车速 / 档位 / 手刹 / 点火状态，并附上五项车辆权限
  的授予情况（读不到时能区分「没这个属性」和「没有权限」）。
- **4.6 logcat 信号线索**。EVCam 匹配的是 `data1 = <数字>` 和
  `front turn signal:`，这里放宽到 turn signal / indcr / door / gear /
  speed，扫最近 3000 行。

  > **采集建议**：这一节要有信号事件才会有内容。请先打几次转向灯、开关一次
  > 车门，**然后立刻**导出报告。

### 开发

- 新增 `tools/check_java_syntax.py`：开发机上没有 JDK 与 Android SDK，
  编译只能靠 CI，一个漏掉的花括号要浪费一整轮。这个脚本检查括号 / 引号 /
  注释配平，推送前先跑一遍。过不了它就一定编译不了。

## [0.5.0-alpha] - 2026-08-28

### 新增

- **连续时间轴回放（实验性）**。侧边菜单 →「连续回放（实验）」。
  把连续录制的分段拼成一条时间轴，拖进度条可以**跨文件**定位 ——
  行车记录仪的实际用法是「看 20 分钟前那段」，不该让用户去猜它在第几个文件里。
  - 按文件名 `yyyyMMdd_HHmmss` 取起始时刻排序，空隙小于 5 秒算连续；
    停止录制再开始会留下明显断档，自动分成两条时间轴。
  - 时间轴换算是纯逻辑、**有 11 条单元测试**（边界落在接缝上、越界夹紧、
    乱序输入、时长读不出的文件、最后一段不足整段）。这类错误表现为
    「拖到这里却播了别处」，在车上靠肉眼几乎发现不了。
  - 与原有回放界面**并存**，不是替换。
  - 已知限制：用的是 VideoView/MediaPlayer，切文件要重新 prepare，
    段与段之间有零点几秒停顿。无缝需要换 ExoPlayer，APK 会明显变大 ——
    先看这个形态够不够用。

- **相机映射**（设置 → 录制 → 相机映射）。可手动指定每个槽位用哪个相机。
  自动分配是「合成流按能力找、其余按 id 顺序补」，后者只是猜 ——
  Camera2 分不出哪路是后排、哪路是驾驶位。这替代了此前删掉的「自定义」入口：
  那本是旧银河车型的布局编辑器，而它在本项目里实际被用来「让另外两路显示出来」，
  现在直接支持这件事。

- **悬浮窗布局重置**（设置 → 界面 → 悬浮窗布局）。
  一键把两个悬浮窗的位置、大小、透明度、字号恢复默认。

### 修复

- **录制悬浮按钮的位置不会被记住**，每次启动都回到屏幕左侧中间 ——
  上游只在内存里改 layoutParams，从来没有落盘（主屏悬浮窗本来就会存）。
  现在拖动结束时保存（只在 ACTION_UP，避免拖动过程反复写），
  启动时恢复并夹紧到屏幕范围内。

## [0.4.4-alpha] - 2026-08-28

### 修复

- **低帧率录制的视频回放速度不对**（实车确认：10fps 录的片段播放快约 2.5 倍）。
  写进文件的时间戳是按「帧计数 × 写死的 40000us（25fps）」算的，
  真实采集时间戳被完全丢弃。于是 10fps 采到的 10 帧被标成只跨越 400ms 而不是 1000ms。
  这个问题一直存在：默认 30fps 时文件其实偏慢 1.2 倍，只是不易察觉。
  现在时间戳步长跟随实际帧率。仍沿用按帧计数递推（保证严格单调递增、不会乱序），
  只是步长不再写死。
  **修复后录出的文件在任何播放器里速度都正确**，不需要额外的倍速播放选项。

### 新增

- **预览分辨率可选**（640x480 / 1280x720 / 1600x900 / 1920x1080）。
  「预览用低分辨率」之前写死挑最接近 640x480 的尺寸，在车机大屏上太糊。
  实际仍从 HAL **已声明**的尺寸里挑最接近的，挑不到就退回录制尺寸。
- 诊断报告增加 **`getConcurrentCameraIds()`** 查询 —— 系统直接声明哪些相机组合
  可以同时打开，用于判断「合成流 + 两路座舱」这个组合到底受不受支持。

## [0.4.3-alpha] - 2026-08-28

### 修复

- **录制帧率选项不生效**（实车确认：选 10fps 录出来仍然很流畅）。
  原因是设置只传到了 `MediaFormat.KEY_FRAME_RATE`，而这个键对 **Surface 输入的
  编码器只是码率分配提示，不会丢帧** —— 实际出帧多少取决于 GL 侧隔多久
  `eglSwapBuffers` 一次。而 `EglSurfaceEncoder` 把这个间隔**写死成 33ms（30fps）**，
  且根本没有 `setFrameRate` 方法。
  现在帧率会同时下发到 GL 侧的出帧节奏，两处用同一个值。

### 说明

- 上述修复覆盖 **MediaCodec** 路径，也就是默认的「四宫格」录制
  （四宫格必须走 MediaCodec）。若切到「原始长条」且录制模式为 MediaRecorder，
  帧率仍可能不生效 —— 那条路径的实际帧率由 Camera2 的
  `CONTROL_AE_TARGET_FPS_RANGE` 决定，本项目尚未设置该参数。

## [0.4.2-alpha] - 2026-08-28

### 修复（涉及内置存储寿命，重要）

- **「中转写入」开关显示关闭，实际仍在走内部存储中转。**
  真正决定落盘位置的 `shouldUseRelayWrite()` 直接读偏好、默认值写死为 `true`，
  而支撑设置开关的 `isRelayWriteEnabled()` 已改为默认 `false`。
  结果是全新安装、选择 U 盘录制时，开关显示「关闭」，但每个分段仍然先写进内部缓存
  再拷到 U 盘 —— 按 200 MB/min 算，**每小时约 12 GB 本不该发生的内置存储写入**。
  两处现在走同一个入口，只保留一个默认值。

- **存储位置显示「未检测到」但实际可选可用**。
  卷枚举只走了 `getExternalFilesDirs()`，而这台车机的 U 盘只能通过 `/proc/mounts`
  看到（这也是原有检测逻辑真正生效的那一步）。现在先扫 `/proc/mounts`
  再合并 `getExternalFilesDirs()`，按根路径去重。

- **切换车型后复用了旧的摄像头映射**。`CameraManagerHolder` 是单例，
  前台服务让进程一直活着，切车型只提示「重启应用后生效」，
  于是旧配置的摄像头映射被原样复用 —— 表现为画面全黑。
  现在 holder 会记录自己是按哪个车型建的，车型变了就重建；
  MainActivity 也会拒绝复用路数与当前配置不符的实例。

- **合成流的目标分辨率不再污染全局配置**（见 0.4.1，此版本继续修正相关路径）。

### 说明

- 3 路配置画面全黑**仍未定论**。已确认车机允许三路并发，
  所以剩下的候选是座舱两路的 id 取错了（目前按 id 顺序猜）。
  诊断页第 2.1 节会列出实际分配，请导出报告以便定位。

## [0.4.1-alpha] - 2026-08-28

### 修复

- **合成流的目标分辨率不再写进全局配置**。原来两条极氪路径都是调用
  `appConfig.setTargetResolution()` 设定合成流尺寸，而这是个**全局**设置，
  `SingleCamera.chooseOptimalSize()` 对**每一路**相机都会读它。
  在 3 路配置里，这等于命令两路座舱相机也去够 `1280x5140` —— 它们根本不支持，
  只能按像素距离退到一个莫名其妙的尺寸。它还会在每次启动时**覆盖掉用户的「画质设置」**。
  现在改为每台相机各自的 `preferredSize`：只有合成流那一路会被指定，
  且只在 HAL 确实声明过该尺寸时才生效，否则记一条警告并退回全局配置。

### 新增

- 诊断报告增加 **「2.1 多路配置的相机分配」**：直接列出 3 路配置会把哪个相机
  分给环视、座舱 1、座舱 2，以及排查时该看什么。

### 说明

- 3 路配置画面全黑的问题**尚未定论**。上面的全局分辨率污染是确凿的 bug，
  但未必是全部原因。另外两个候选：座舱两路的 id 取错了（目前是按 id 顺序猜的），
  或者车机不允许合成流与另外两路相机同时打开。
  用其他车型配置能显示这两路，说明相机本身可用 —— 问题在分配或并发组合上。
  请用诊断页导出报告以便进一步定位。

## [0.4.0-alpha] - 2026-08-28

### 变更

- **设置页重排：32 项精简为 28 项，分成 5 组**
  —— 录制 / 存储 / 后台与保活 / 界面 / 高级与诊断。
  原来是一条 32 项的直筒滚动，沿用自 EVCam 的多车型设计。
- **「车型配置」改名「视频流配置」**。在本项目里它选的是用哪几路视频流
  （仅环视 / 环视+后排+驾驶位），而不是选车型 —— 原来的说明文字正好说反了。
- **「录制模式」在选择四宫格时自动隐藏**。四宫格必须走 MediaCodec，
  此时该选项会被忽略，露出来只会让人以为可以改。

### 移除

- **预览画面矫正 / 鱼眼矫正** —— 这两项通过 `textureView.setTransform()` 生效，
  而合成流分支根本不走那条路（`FourLaneContainer` 自己排版）。
  在极氪配置下它们**不产生任何效果**。
- **定制键唤醒** —— 副标题写着「仅限龙鹰一号车型」，且依赖已被换成空实现的
  `VhalSignalObserver`，永远不会触发。
- **检查更新** —— 指向上游的更新渠道；本项目走 GitHub Release。
  连同 `VersionUpdateManager` 一并删除。
- **自定义界面配置** —— 仅自定义/多视角车型使用，这些车型已隐藏。
- **首次启动引导里的打赏二维码** —— 那是 EVCam 作者的收款码，
  且是运行时从其服务器拉取的；在改名后的分支里展示会让用户误以为是在支持本应用。
  这也是移除远程模块后**最后一个残留的对外网络请求**。
  对上游的致谢与链接保留在「关于与致谢」页面。

### 修复

- **第三路摄像头永远没有复选框**：显示条件写的是 `cameraCount >= 4`，
  而新的「环视+座舱3路」配置正好是 3 摄，导致第三路无法勾选或取消。
- **移除自定义配置按钮会让整个车型配置失效**：它的初始化把该按钮算进了
  null 检查，按钮不存在就直接 return 了。
- 「录制摄像头」的说明里还写着早已删除的「钉钉指令」。

### 说明

- 「录制摄像头」按讨论结果**暂时保留**，后续再定。

## [0.3.1-alpha] - 2026-08-27

### 修复

- **存储位置首次进入时显示为「未检测到 U 盘」，退出再进才正常**，
  且会连带把下方的「中转写入」选项一起藏掉。
  原因是有**两段异步检测在赛跑**：旧的那段用写死的「内部存储 / U盘」两项重建选择器，
  新的那段用真实卷列表重建，谁后完成谁说了算。更关键的是旧那段在 `setAdapter` 之前
  没有置初始化标志 —— Spinner 换 adapter 时会先回调一次 `onItemSelected(0)`，
  被当作用户主动选了「内部存储」写进配置，中转写入行随之隐藏。
  现在合并为一次检测：枚举卷 → 由卷列表推导「有无外置存储」→ 带初始化标志重建选择器
  → 再刷新描述文字与中转写入可见性。初始化期间的回调一律不写配置。

### 变更

- **中转写入默认关闭**。它会先写内部存储再搬到 U 盘，闪存写入量翻倍；
  只有在 U 盘写入速度确实跟不上、录制出现卡顿时才值得开启。

## [0.3.0-alpha] - 2026-08-27

### 新增

- **诊断信息页**（菜单 → 诊断信息）。一次性采集相机能力、屏幕列表、车辆信号来源、
  存储卷与近期日志，支持**保存为文件 / 复制到剪贴板 / 分享**三种导出方式。
  车机上逐项手工排查太慢，这一页能一次回答「有几路相机」「有没有副屏」
  「转向灯信号读不读得到」「挂了几个盘」。
- **多存储卷选择**。之前存储位置写死为「内部存储 / 一个 U 盘」，
  实现上取的是第一个检测到的外置卷 —— 插两个盘时第二个永远选不到。
  现在枚举所有卷，每个盘单独列一项并显示剩余/总容量。
- **极氪7X（环视+座舱3路）配置**（非默认）。环视合成流 + 两路座舱摄像头，
  左侧 2×2 环视、右侧上下两路座舱。哪路是后座、哪路是主驾无法从 Camera2 判断
  （车机相机通常都报 EXTERNAL），因此按 id 顺序标为「座舱 1 / 座舱 2」，
  待实车确认后再调整。

### 修复

- 在两个外置卷之间切换会被「没变化」判断误吞（该判断只比较 internal/external）。
- 存储列表的异步刷新只在「有没有外置盘」这个布尔翻转时才重建；
  在已有一个盘的基础上再插一个时该布尔不变，列表永远不更新。

## [0.2.0-alpha] - 2026-08-27

### 新增

- **录制与拍照改为 2×2 四宫格输出**。此前录出来的是车机原生的竖向长条
  （四个画面一字排开）。顺带解决一个实际问题：合成流高 5140，
  超过编码器 4096 的上限，原路径会把整帧缩小；2×2 输出（2560×2560）
  像素数相同却落在限制之内，因此不再缩放。
  - 视频：`EglSurfaceEncoder` 改为绘制四个子区域到 2×2 格子；
    `CodecVideoRecorder` 把相机源尺寸与编码输出尺寸解耦。
  - 照片：新增 `CompositeBitmapComposer`，用 Canvas 重排，与预览、录制共用同一套几何。
  - 水印单独走一遍 2D 叠加 —— 原水印着色器靠纹理坐标判断水印区域，
    四宫格下每个格子只采样纹理的一个子区间，判断会打在错误位置。
  - 设置项：**录制画面排列**（四宫格 / 原始长条），默认四宫格。
- **录制帧率选项**：原始帧率 / 30 / 24 / 20 / 15 / 10 fps。
  上游只有「标准/低」两档且没有任何设置入口，基准值写死为 25fps。
- **预览用低分辨率（实验性，默认关闭）**：预览跑小分辨率、录制仍用完整分辨率，
  可降低 GPU 与内存带宽。能否生效取决于车机 HAL。

### 修复

- **悬浮录制按钮拖动范围受限**。原因是 `screenWidth - maxWidth` 恒等于 200，
  X 被锁死在 0–200，屏幕多宽都没用（Y 的写法是对的）。
  现改为夹在屏幕边界内 —— 可到任意位置，但不会被拖出屏幕。

### 变更

- **默认车型改为极氪7X**，车型选择只保留极氪配置，
  银河 / 星舰 / 手机 / 自定义等上游车型已隐藏（代码保留，恢复只需改回一个数组）。
- 车型选择器由硬编码下标改为值数组映射，增删选项不再需要重新编号。

## [0.1.1-alpha] - 2026-08-27

### 变更

- 应用中文名定为「极氪即刻（车机版）」，成为默认 launcher 名称；
  英文环境下仍显示 `Zeekr Shortcut`（新增 `values-en/strings.xml`）。

### 修复

- CI 的版本号提取会匹配到注释行（注释里含 `versionName` 字样），
  导致 APK 文件名里混入换行、构建失败。改为锚定到赋值语句本身。

## [0.1.0-alpha] - 2026-08-27

### 变更（重要）

- **四宫格渲染架构整体重写：从 OpenGL 改为父容器重画子视图。**
  深入阅读 openavm-recorder 公开源码后发现一条关键实测结论：用 GL 自建的
  SurfaceTexture 顶替原本正常工作的相机生产者，**在真车上会导致预览崩溃**；
  其验证可行的结构是「Camera2 只喂一个普通 TextureView，由父容器把该子视图
  重复画进四个格子」。本项目原先的 `CompositeTextureView` 正是前一种做法，
  已删除并由 `FourLaneContainer` 取代。相机链路现在完全不被改造。
- 拆分几何改为按**合成流真实尺寸**计算归一化窗口，再套到实际绘制区域。
  这样 HAL 给出压扁的小尺寸提示（如 640×480）时，四个画面的位置与比例仍然正确。
- 新增 `FourLaneContainer.DisplayMode.RAW`：不拆分、原样显示整条合成流，用于排查问题。

### 移除（重要）

- **裁掉全部用不到的模块，APK 从 24.4 MB 降到约 16 MB。** 这些功能面向吉利银河车型，
  与极氪合成流场景无关：
  - 钉钉 / 飞书 / Telegram 远程查看、心跳推图（连同 DingTalk Stream SDK）
  - MJPEG / RTP / UDP 推流（连同 NanoHTTPD）
  - 二维码扫码互传（连同 ZXing）
  - VHAL 车辆信号（连同 gRPC ×2 与原生 .so）
- 共删除 58 个源文件，MainActivity 减少约 1300 行。移除按「整个成员」粒度进行，
  不会把方法切成两半；生命周期与核心方法为手工编辑而非删除。
- `VhalSignalObserver` 换成公开 API 完全一致的空实现，因此补盲模块无需改动即可编译。
- 所有被移除的代码**完整保留在 git 历史中**作为参考实现。

### 修正

- **U 盘存储可用。** 此前依据 openavm-recorder 的记录标注为「App Lab 不允许直接写
  外置 USB 存储」。本项目作者已在极氪车机上实测：U 盘写入可以正常工作。
  该结论因此不适用于本项目，U 盘存储按可用对待。

### 新增

- 首个版本：极氪7X 环视合成流支持，代码基座为 EVCam `0876b97`（GPL-3.0）。
- [`docs/zeekr-platform-notes.md`](docs/zeekr-platform-notes.md)：极氪 App Lab
  平台事实笔记，逐条标注【实测】/【未知】及本项目据此做的决定。
