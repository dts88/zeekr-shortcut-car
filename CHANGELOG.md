# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

每个版本的小节会被 CI 自动摘出来，作为该版本 GitHub Release 的说明正文，
所以这里写的内容就是用户在 Release 页面看到的内容。

## [Unreleased]

（暂无）

## [0.36.2-alpha] - 2026-09-03

### 车牌号

设置 → 录制 里新增一个开关和一个输入框。最长十位，只能是大写字母和数字 ——
输入的东西会被自动整理（小写转大写，空格、连字符、汉字去掉，超长截断），
并把整理后的结果直接显示在下面 —— 录进画面的就是那一串。

开关关着时一定不录。这是车主自己的信息，要不要落在画面里应当由他决定。

### 照片角标和录像对齐了

以前照片只有一行时间，而录像有应用名、版本、尺寸 —— 同一台设备记录的
两种东西，角标写的内容却对不上。现在照片也是三行：

```
极氪即刻 v0.36.2-alpha  A12345
2026-09-03 14:22:07
2560x2560
```

两边共用同一个拼字符串的函数（{@code WatermarkText}，有单元测试）。
照片没有帧率、码率、编码，那几项就不写 —— 为了「看起来一致」硬凑几个数，
比不写更糟。

### 「录制码率」切换后摘要消失

写摘要的代码跑在 {@code setValue} 之前，拿到的还是上一个选项，
而且紧接着就被默认的 setSummary 盖掉了。现在把「怎么写摘要」作为一个
参数交给 bindEnum，在 setValue 之后调。

### 目标码率按<b>真正在录的那个尺寸</b>算

上一版拿「录制分辨率」那个设置去算 —— 而环视这一路根本不读它，
四宫格重排还会把 1280×5140 拼成 2560×2560。现在直接问正在跑的那台相机，
再套用录制链路同一个 {@code EncodeSize}（新抽出来的纯函数，有单元测试）。

### 「原始帧率」右边写上视频流声明的帧率

这一档的含义是不限制、跟随视频流，那么主视频流能给多少就是它实际会得到多少。
现在去读主视频流（极氪 7X 上是环视合成流）声明的帧率写在旁边。读不到就不写。

### 「录制分辨率」在环视配置下本来就不生效

极氪 7X（单路合成流）只录环视这一路，它的尺寸由合成流本身决定，
代码里用 setPreferredSize 钉死，全局目标分辨率根本不会被读到。
摆一个能选、选了又不生效的下拉框，比不给这个选项更糟。现在它在这个配置下置灰，
并指向开发者选项里的「环视流尺寸」。「环视 + 两路座舱」不受影响 ——
那两路座舱仍然按这里选的尺寸挑。

## [0.36.1-beta] - 2026-09-03

### 修回 0.36.0-beta 引入的帧率 0

上一版把「原始帧率」改成了「不限制」，用 0 表示。但 0 被原样传给了所有人：

- **编码器**：`KEY_FRAME_RATE` 拿到 0，码率公式算出 `宽×高×0 = 0`，
  再被下限拉到 1.5Mbps —— 无论选什么画质，录出来都是最低码率。
- **相机**：AE 帧率区间的目标是 0，挑区间时会挑到<b>最低</b>的那一个，
  恰好和「不限制」相反。

「不限制」只对渲染节流成立，对其他人不成立。现在拆成两个方法：

- `getFrameRateCap()` — 只给节流用，0 = 不限制。
- `getNominalFrameRate()` — 一定是正数，给编码器、码率估算、AE 区间用。

顺带把写死的「硬件最大 25 帧」换成相机自己声明的值。实测这一路稳在 29–30，
写死 25 的后果是：选 30 被悄悄夹到 25，而界面上还写着 30。

### 界面文字回归朴素

- 「录制帧率上限」改回「录制帧率」，各档不再加「最高」前缀，
  说明性的那一行也去掉了。
- 「应用名与版本角标」改为「应用角标」，注释只留「显示本应用的名称和版本号」。
- 「检查更新」下面那句说明去掉。

### 录像角标不再印目标码率

目标码率是设置里选出来的一个上限，印在画面里没有信息量。角标上的码率现在
只有一个：每秒实测的那个。目标码率移到设置的「码率」那一项下面显示，
带上它依据的分辨率、帧率和编码 —— 算它的和编码器用的是同一个函数
（`TargetBitrate`，有单元测试），不会出现界面一个数、实际另一个数。

### 环视流尺寸：界面上写的必须是实际在用的

换了尺寸重启后，主界面仍然写着 1280×5140——因为那一行写的是探测结果，
不是相机真正在用的尺寸。现在相机开起来之后会用实际尺寸再刷一次，
强制指定过就同时标出来。强制的尺寸如果 HAL 没声明，相机会退回全局配置，
这时界面上写着强制值就是假的。

下拉框也改成列环视那一路<b>自己</b>声明的尺寸。原来用的是三路的交集，
而交集会把 1280×5140 这种只有它才有的条带尺寸筛掉——而那正是默认值。

（开发者模式重启后会退出，这是故意的；但尺寸设置本身存在配置里，不受影响。）

### 下载与分享：失败要看得见

「检查更新」下载时，如果对面没给文件长度，进度回调会直接返回，界面就一直停在
「正在下载…」不动 —— 和「根本没开始」分不出来。现在：

- 连接阶段单独显示「正在连接…」，进度条走不确定动画。
- 没有长度时显示已下载的 MB 数，至少能看见它在涨。
- 失败改用对话框显示具体错误（原来是 toast，一闪而过等于没有）。
- 下载地址、目标路径、完成与失败都写进日志。

分享二维码画不出来时，原来那一格是白的，和界面坏了没区别。现在会写明
「二维码没能生成，请直接输入下面的地址」，日志里也记下可用地址的数量与内容。

## [0.36.0-beta] - 2026-09-03

30 帧录制在车上验证通过（实测 26–30，多数时间 28 左右），
这一版把帧率设置的语义改对，并补上试更高分辨率所需的开关。

### 「原始帧率」现在真的不限制

以前它会先算出一个具体的数（比如 25），再拿这个数去节流 ——
于是名为「原始」的选项反而成了一道限制，而且当这个数和视频流的
出帧节奏不成整数倍时，节流还会把帧率砍一半。

现在：

- **原始帧率** = 不设任何门槛，视频流给多少录多少，一帧不丢。
- **其余各档** = **上限**，不是强制值。视频流本来就给不到那么多时，
  录出来只会更低 —— 选 30 不会把 20 帧的画面补成 30 帧。

选项文字同步改成「最高 30 fps」这样的写法，并在下面加了一行说明。
显示的含义和实际行为现在是同一件事。

### 视频回看不再被状态栏压住

这个界面用的是全屏主题，窗口铺到了状态栏底下，顶部的标题和列表被车机
状态栏遮住一条。改成和「软件设置」一样的普通主题，由系统把状态栏留出来。
全屏主题至此没有任何界面还在用，一并删除。

### 开发者选项：环视流尺寸

环视这一路除了 1280×5140，还声明支持 3840×2160 等尺寸，分辨率实测也确认
这些尺寸都能真的出画面。新增一项可以强制指定这一路用哪个尺寸，用来看看
换成 3840×2160 之后，这一路里装的到底是什么。

摘要里会写明所选尺寸会被怎么处理：长边达到短边 3.2 倍才算条带、才切四格，
3840×2160 是 16:9，达不到这个比例，会当成一整幅画面显示。
不写清楚的话，换完尺寸看到「只有一个画面」很容易以为是坏了。

默认仍是「跟随探测」—— 1280×5140 是已知能出四格竖排的那一个。
这一项放在开发者选项里，改错了改回来即可，需重启应用生效。

## [0.35.0-alpha] - 2026-09-03

### 找到了：帧率是被我们自己砍掉一半的

实测证明相机没有问题 —— 各分辨率、请求与不请求，都稳在 30 帧左右。
问题在录制这一侧，而且是一句看着完全正常的判断：

```java
if (现在 - 上一帧渲染时刻 < 目标间隔) 跳过;
```

**当相机的出帧间隔和目标间隔不成整数倍时，这句话会直接把帧率砍一半：**

```
相机 29fps（每 34.5ms 一帧），目标 25fps（间隔 40ms）

t=0      渲染        记 上一帧=0
t=34.5   34.5 < 40   跳过
t=69     69 ≥ 40     渲染   记 上一帧=69
t=103.5  34.5 < 40   跳过
t=138    渲染
...
每两帧渲一帧 = 14.5 fps
```

这就是「选 25 或 30，录出来 15」。和相机、和自动曝光都无关。

**为什么不能精确压到 25**：整帧丢弃只能得到 源/N 的速率 —— 29fps 的源
只能给出 29、14.5、9.7…，**拿不到 25**。所以真正的问题不是「怎么压准」，
而是「29 和 14.5 哪个更接近 25」。

答案是 29。用户选 25 是想要「大约这么流畅」，不是「不许超过 25」。
所以现在允许 25% 的提前量：不比目标间隔早太多就渲染。

判断搬进 `FrameThrottle`，7 条单元测试，其中一条直接复现了旧写法的 14.5：

| 源 | 目标 | 旧 | 新 |
|---|---|---|---|
| 29 | 25 | **14.5** | **29.0** |
| 30 | 15 | 15.0 | 15.0 |
| 30 | 10 | 10.0 | 10.0 |
| 29 | 30 | 29.0 | 29.0 |

该降的照降，不该腰斩的不再腰斩。

### 更正：上一版关于自动曝光的判断是错的

0.34.0 说「AE 在 15–30 里滑到了下限」。你的实测推翻了它 ——
不请求帧率区间时相机照样跑 29。

那一版加的 `CONTROL_AE_TARGET_FPS_RANGE` 请求本身没有害处（明确请求比听天由命好），
但它**不是**这个问题的原因，也不会是解决它的那一步。

### 「87 帧」是什么

是那 3 秒测试里一共收到的帧数：3 × 29 ≈ 87。结果行现在直接写成
「29.4 fps（3 秒内共收到 87 帧）」，不用再猜。

## [0.34.0-alpha] - 2026-09-03

### 找到「选 30、录出 15」的原因了

**本应用从来没有向相机请求过帧率区间。**
`CONTROL_AE_TARGET_FPS_RANGE` 这一项，全项目搜下来一次都没设过。

不请求，就等于把帧率交给自动曝光在 HAL 的默认区间里自己决定。
而这台车机声明的是 **15–30** —— 一个可变区间。曝光一紧张、负载一上来，
AE 完全有权滑到下限 15，而且它不需要通知任何人。

**相机不是给不到 30，是没人要求过它保持 30。**

这也解释了为什么预览测出 29、录制只有 15：预览负载轻，AE 乐意跑在上端；
一开始录制，同一条流上多了编码器，AE 就退到了它允许的最低点。

现在会明确请求一个区间，挑选规则（`FpsRangePicker`，7 条单元测试）：

1. **固定区间 `[n,n]`** 最优先 —— AE 没有滑动余地；
2. 没有固定区间时，取**包含目标且下限最高**的那个，把可滑动空间压到最小；
3. 目标不在任何区间内时，取上限最接近的；
4. 相机没声明区间就**什么都不设** —— 硬塞一个没声明的值会让会话配置失败，
   那是把「帧率不对」变成「根本没有画面」。

按这台车机只声明了 15–30 来看，请求之后 AE 仍有下滑的余地，
所以日志里会明确标出「可变区间，自动曝光仍可能下滑到下限」。
能不能真的稳在 30，得看实测。

### 修：分辨率实测数不到帧

连 1280×5140 这种一定出得来的流都报「无画面」。

原因：它给 TextureView 的 SurfaceTexture 装了 `setOnFrameAvailableListener` ——
**那会顶掉 TextureView 自己的那个回调**，于是没有人再调 `updateTexImage()`；
缓冲区几帧就排满，生产者被卡住，回调随即停止。

改成两个输出各司其职：**ImageReader 负责计数**（拿到就立刻关掉，不堆积），
**TextureView 负责显示**，它自己的回调不再被动。

### 分辨率实测加了帧率这一维

多一个「帧率区间」下拉：**不请求（相机自己决定）** + 相机声明的每一个区间，
固定/可变都标出来。

分辨率和帧率是两个变量。「30 帧跑不到」可能是这个尺寸带不动，
也可能是根本没请求过 —— 分开试才分得清。

第一项「不请求」保留的正是本应用此前的行为，方便和明确请求的结果对照。
结果行会写明本次请求的是哪个区间。

## [0.33.0-alpha] - 2026-09-03

### 修：实测帧率一次都没算出来过

角标始终显示「~25fps」——那个波浪号的意思就是「还没测出来」。

原因：统计挂在写 muxer 的地方，按「每 300 帧算一次」。而**写 muxer 有两条路径**，
那段判断只在其中一条里。帧数从另一条路走过去时计数照加、判断照跳，
于是 `帧数 % 300 == 0` 那一刻可能永远撞不上。

统计搬到 `noteEncodedBytes()` —— **两条路径都会调它**，和实时码率共用同一个一秒窗口。
换分段时也不再把实测值清零：重建的是编码器，相机的出帧率不会因此改变。

### 修：「原始帧率（25 fps）」这个 25 和相机无关

那是代码里写死的常量。而这台车机上环视这一路**声明 15–30、实际送出 29** ——
界面上那个 25 既不是相机能给的，也不是我们真录到的，两头都不沾。

现在相机打开时把它声明的最高帧率记下来，设置界面用这个数。
**读不到就不写数字**，显示「跟随相机（尚未探测到）」——与其编一个，不如说实话。

### 开发者选项 → 分辨率实测

相机**声明**支持某个尺寸，不等于它在这台车机上真的给得出画面。
尤其是 EXTERNAL 那一路——它不是一颗传感器，而是车机送进来的一路既成视频，
声明里那一串常规尺寸很可能只是 HAL 的模板。

这一屏对每个尺寸**真的配置一次会话、真的出一次画面**，逐行报告：

```
3840×2160    正常  29.0 fps，87 帧
1280×5140    正常  29.1 fps，87 帧
2560×1440    无画面  会话建起来了但一帧都没来（5031 ms）
1920×1080    失败  会话配置失败
```

「测这一个」看单个尺寸的实时预览，「全部依次测」跑完整张清单。

**它会占用相机**：一台相机同一时刻只能被打开一次，所以开始前会先把应用自己的
那份放掉，否则每个尺寸都会以「被占用」失败，看起来像声明全是假的。
测完回主界面会重新初始化。**正在录制时不要进来。**

## [0.32.1-alpha] - 2026-09-03

### 从「相机出帧」开始测

新增**不依赖录制**的出帧率统计。预览的 `onSurfaceTextureUpdated` 每来一帧回调一次，
数它就得到相机的实际出帧率 —— 这是**这条视频流本身的上限**，录制只会更低。

之前只在编码器里统计，那必须先开始录制才有数；而录制本身是开销最大的一件事 ——
用一个带开销的过程去测另一个的上限，测出来的永远是两者的合力。

结果出现在两处：**开发者选项 → 相机能力清单**（最上面，带刷新），
以及**诊断报告 2.2 节**。

判读方式：
- 这个数就是 15 左右 → 相机给不到更多，设置里调到 25 也没有意义；
- 这个数是 25 而录制只有 15 → 是渲染跟不上，日志里
  「相机送来 X fps，实际渲染 Y fps」那一行会直接标出来。

### 相机能力清单补上名词解释

- **硬件级别** LEGACY < LIMITED < FULL < LEVEL_3 —— 越高能手动控制的越多。
  LIMITED 表示没有逐帧手动曝光/对焦，对本应用没有影响。
- **三种输出格式**：同一份画面按用途分成三条清单 ——
  PRIVATE 给编码器（录制走它）、SurfaceTexture 给显示（预览与后视镜走它）、
  JPEG 是相机直接给一张压好的照片（拍照通道）。三条尺寸常常一样，
  说明这一路在三种用途下能力相同。
- **帧率范围「未声明」是什么意思**：EXTERNAL 类型（车机送进来的视频流）常常不声明 ——
  它不是一颗受控的传感器，而是一路既成的视频。这时唯一可信的就是实测出帧率。
- **环视合成流为什么是 5 条分隔带**：1280×5140 = 4×1280 + 20，
  20 行是 5 条 4px。不是 3 条，因为上边缘和下边缘各还有一条：
  上 1 + 中间 3 + 下 1 = 5。拆四宫格时必须都算进去，否则每格都带黑边，越往下偏得越多。

### 拍照通道的现状（写进了清单里）

本应用现在的拍照是从预览画面上抓一帧，所以照片清晰度受限于**预览尺寸**，
而不是 JPEG 栏里的最大值；照片里也没有任何 EXIF —— 它本来就不是相机拍的。

## [0.32.0-alpha] - 2026-09-03

### 开发者选项 → 相机能力清单

把诊断报告里那几行相机数据摊开成一屏，每一项都写清楚**它决定什么**：

- **SurfaceTexture 尺寸** —— 预览与录制能选的档位，按像素从大到小排，
  每一行带宽高比和百万像素数，环视合成流那一档单独标出来。
  「除了 1280×5140 还有哪些能用」看这一栏。
- **JPEG 尺寸** —— 相机拍照能出的档位。
- **帧率范围** —— 相机声明能跑的区间与其中的最高值。
- **本应用当前实际在用** —— 每一路当前的相机 id、预览尺寸、缓冲区尺寸。
- 末尾一节解释这四类数字分别决定什么。

只读，不改设置，也不动正在跑的相机会话。

### 帧率：把两个数分开测

日志里每 5 秒记一条：**相机送来多少帧** 与 **实际渲染多少帧**，
渲染明显低于送来时标一句「渲染跟不上」。

「录出来只有 15fps」有两种完全不同的原因：相机就只给了 15 帧，
或者相机给了 25 帧而我们只渲染得出 15 帧。前者是这条视频流本身的上限，
改设置没有用；后者是开销问题，降分辨率或关水印可能就好了。
**只看最终文件分不出这两种**，所以分开测。

## [0.31.3-alpha] - 2026-09-03

### 修：分享测试界面进去就出不来

那个界面没有任何退出方式，而车机上没有系统返回键。最上面加了「← 返回」。

### 修：角标上的帧率是标称值，不是录到的帧率

分享到手机后，播放器读出来 15.29 fps，而画面角上印着 25 fps —— **印在画面里的那个是错的**。

设置里选的帧率是**渲染节流的上限**，不是结果：相机给不到那么多帧，编码器就出不到
那么多帧。代码里其实一直在算实测帧率（每 300 帧记一次日志），只是没接到角标上。

现在角标写**实测值**。测出来之前先写 `~25fps` 带个波浪号，避免刚开始录时角标是空的。

顺带说明：设置里「原始帧率（25 fps）」那个 25 是代码里写死的假设，不是从相机读来的。
这一版还没改它 —— 得先知道相机到底声明了什么，见下一条。

### 诊断报告加上相机的帧率范围

每个相机现在会列出 `CONTROL_AE_AVAILABLE_TARGET_FPS_RANGES` 与其中的最高值。
「这几路最高能跑多少帧」以前报告里根本没有，只能靠猜。

### 分享对话框补了两条提示

- **网络**：需要手机和车机在同一个网络里，最省事的做法是用手机开热点让车机连上。
  没有可用地址时的那个弹窗也带上同一句。
- **视频只发当前这一段**：录像按每 N 分钟存一段，连续回放是把多段接起来放的，
  而分享只发当前所在的那一段；要发别的时间段，把进度拖过去再点一次。
  N 取自设置里的实际分段时长，不是写死的。

两条都有中英文。

### 应用名与版本角标：摆到设置里，灰着

录像左上角的应用名 + 版本号本来就是无条件盖上去的。现在设置里能看见这一条：
**开着、灰着、点不动**。给一个能关的开关，等于承诺一件代码里并不打算允许的事。

## [0.31.2-alpha] - 2026-09-02

### 局域网地址进「诊断信息」

**设置 → 关于 → 诊断信息** 里新增一节「发送到手机（局域网）」，导出报告时一并带上：

- 检测到的每一个 IPv4 地址，连同**网卡名**和**判定类别**（HOTSPOT / WIFI / TETHER / OTHER），
  排第一的就是默认会用的那个；
- **端口能不能监听** —— 试着让系统分配一个端口再立刻放掉。

分成这两项是因为扫码打不开时，问题可能在三个地方：**根本没有可用地址**、
**地址挑错了**、或者**端口起不来**。这三件事在车上都看不出来，从截图更看不出来，
只有让报告分开写清楚才好判断。

报告随时可以导出，不要求分享服务正在运行 —— 这一节不碰服务实例，
只列地址、试端口，不干扰任何正在进行的事。

JSON 导出里也有，因为它内嵌了同一份文本报告。

### 测试界面的日志写全了

**开发者选项 → 发送到手机 · 连通性测试** 界面上显示的每一条，
现在日志里都有一条对应的：地址列表（含网卡名与判定）、手动切换、
二维码地址与文件大小、二维码生成失败。

界面上的东西截图截不全，而日志可以整份导出（设置 → 高级 → 保存日志）。
两边和「诊断信息」那一节对得上，才好判断是网络问题还是应用问题。

## [0.31.1-alpha] - 2026-09-02

> 仍是 alpha：这条链路的前提（两台设备直连）还没在车上验过。
> 先把界面接上，好让两件事能一起验。

### 回放界面上的「发送到手机」

**图片回看**：底部控制栏加了一个「发送到手机」，发的是**当前放大显示的那一张**。
四宫格模式下不发 —— 那时屏幕上是四张图，「这一张」没有定义，
与其挑一张替用户做主，不如提示先双击放大。

**连续回放**：控制栏加了同样一个按钮，发的是**当前播放位置所在的那一个分段文件**，
不是整条时间轴。一条时间轴是好几个分段接起来的，动辄几个 G，
而人想要的通常就是刚看到的那一段。

点下去会起一个临时的局域网服务并显示二维码：
手机扫码 → 浏览器打开这个文件 → 长按保存。

- **地址可以换**：车机开热点和车机连手机热点，能用的是不同网卡上的地址。
  检测到多个时对话框上有「换一个地址」，扫不通就换一个再扫。
- **服务只在对话框开着时活着**，关掉即停。所以对话框里写明了
  「保存完成前请不要关闭本窗口」—— 一个能被局域网访问的文件服务，
  没有理由在用户已经不看它的时候继续开着。
- 没有可用地址时不报错，而是说清楚该先做什么（建立热点连接）。

### 顺带

连续回放里长按会话弹出的两个对话框漏了 `AlertDialogTheme` —— 这个主题下按钮文字
和背景同色，看起来就是「弹出来了但没有按钮」。项目里为此改过几轮，这是又两个漏网的。

## [0.31.0-alpha] - 2026-09-02

> alpha：只加了开发者选项里的探索界面，回放界面上还没有分享按钮。
> 检查更新不会推送 alpha，需要手动侧载。

### 「发送到手机」第一步：连通性探索

**开发者选项 → 发送到手机 · 连通性测试**

先做探索、不做界面按钮，是因为整件事的前提是**两台设备在同一个局域网里直连**。
这个前提在这台车机上成不成立、成立时该用哪块网卡的地址，代码里看不出来，只能在车上试。
所以先把「有哪些地址、能不能起服务、手机扫码打不打得开」三件事单独摆出来。

### 调研：两个上游项目都做过类似的事

- **EVCam**（GPL-3.0，本项目的代码基座）**做过一模一样的功能** ——
  `transfer/FileTransferServer` + `QrCodeGenerator` + `QrTransferDialog`，
  在早先那次精简里被整体移除。做法是 NanoHTTPD 起 HTTP 服务、ZXing 生成二维码、
  遍历 `NetworkInterface` 找热点网卡的 IP。**这条路直接沿用。**
- **openavm-recorder** 的 README 记载它支持「本地热点 / 局域网传到 OpenAVM Companion」，
  并注明「已在手机热点下测试过配对与下载」。但它走的是**配套 App**，不是浏览器。
  该项目保留所有权利，本项目一行代码都不会抄；这里只取一个事实：
  **热点直传在这类车机上是行得通的**。

### 这一版加了什么

- **`LocalNetwork`**：列出所有可用 IPv4 地址，按「更可能是那条直连」排序。
  车机开热点时能用的是 AP 那块网卡（`ap0` / `softap0`），车机连手机热点时能用的是
  `wlan0` —— **两种连法地址不在同一块网卡上**，所以不能只认一种。
  全部列出、不隐藏，猜错时可以自己挑一个试。判断与排序是纯函数，8 条单元测试。
- **`FileShareServer`**：一次只放**一个**文件，路径带一段随机串，用完即关。
  落地页直接把图片 / 视频显示出来并写明「长按保存」，另附一个直接下载的链接。
  不做文件浏览器 —— 把整块盘暴露到局域网上是另一回事，风险也完全不同。
- **`QrCode`**：ZXing 封装。
- **测试界面**：列出检测到的地址、可逐个切换、分享最新的一张照片或一段录像、显示二维码。

### 下一步（等这一屏在车上验证通过再做）

回放界面上的分享按钮。视频按**当前播放位置所在的那一个分段文件**分享，
不是整条时间轴。

## [0.30.0-beta] - 2026-09-02

**本项目的第一个 beta 版。**

### 检查更新只推 beta 与正式版

alpha 是开发过程中随手发的，数量多、稳定性没有保证 —— 不该被推给一台正在用的车机。
现在检查更新只考虑 beta 和正式版，其余一律跳过。

后缀既不是空也不是 beta 的都不算，**包括 rc**。这不是疏忽：真要发 rc，
应当在代码里明确加上那一档，而不是靠「看起来比 beta 新」这种默认放行。

同号版本之间的新旧仍按语义化版本：alpha < beta < 正式版。

### 修：双指缩放后的尺寸从来没有被保存过

设置里的「窗口宽度 / 高度」和眼前那个窗口对不上，根因不是设置页读得不对，
而是**捏合改完的尺寸压根没落盘**。

手势的实际顺序是：按下 → 第二根手指按下（开始缩放）→ 移动（窗口跟着变大变小）→
**抬起第二根手指** → 抬起第一根手指。

抬起第二根手指时，代码只把 `pinching` 置回 false 就返回了，打算留给最后的
`ACTION_UP` 统一处理；可等到那时 `pinching` 已经是 false、`dragging` 也一直是 false，
落盘那道判断走不进去 —— `savePosition()` 根本不会被调用。

于是窗口当场变了大小，配置里还是旧值。下次重开后视镜会跳回旧尺寸，
设置页的两根滑块也永远显示旧数。

现在缩放结束时就落盘。同时记一个「这次触摸里缩放过」的标记，
好让松手时该把窗口收回屏幕内的逻辑照常执行。

### 「窗口宽度 / 高度」每次进入都重读

后视镜是悬浮窗，**可以在设置页开着的时候被捏大捏小**。滑块的值原先只在界面构建时
读一次，之后再没更新过。现在回到这个界面时重读一遍。

## [0.29.1-alpha] - 2026-09-02

### 修：拖过窗口大小之后，后视镜就一直是糊的

`TextureView` 在尺寸变化时会把自己 SurfaceTexture 的默认缓冲区尺寸设成**自己的布局尺寸**。
后视镜窗口是可以拖、可以捏、可以在设置里拉滑块的 —— 每改一次大小，
相机就不再输出 1280×5140 的合成流，而是输出一张**窗口那么大**的图；
后视镜再从这张小图里取四分之一放大回整个窗口。

于是一个 800×600 的窗口，实际是把大约 200×150 的内容放大回 800×600。
而且不会自己恢复：缓冲区尺寸只在绑定相机时设过一次。

`onSurfaceTextureSizeChanged` 这个回调原本是空的 —— 它正是为这件事准备的。
现在在那里把缓冲区尺寸拨回相机会话配置时用的那个。

`SingleCamera` 在创建会话前也做同样的事（那里有注释说明为什么要做），
但会话只创建一次，之后每一次缩放都得由服务这一侧兜住。

### 说明补充：校正与视野角度也影响清晰度

这两条是设计使然，不是 bug，但设置项里原先没说清楚：

- **鱼眼校正**（默认开）会放大画面中心 —— 校正后的边缘对应偏离光轴 fov/2 的射线，
  也就是只取到原始画面半径的 fov/180。110° 时约取中心 61%，等于 1.6 倍线性放大。
- **视野角度**因此也是一个清晰度开关：角度越大，取到的范围越大、放大倍数越小、
  画面越清晰，代价是边缘拉伸更明显。

两处摘要都改写了，把这层关系写在了界面上。

### 顺带核对过、确认没有问题的

- **码率与帧率与后视镜无关**：它是相机会话的一路预览输出，不经过编码器；
  预览也没有单独设置帧率范围，走 HAL 默认。
- 合成流尺寸在前台和后台两条初始化路径上都有 `setPreferredSize` 固定，
  不会退回全局「录制分辨率」。
- 「预览用低分辨率」默认关闭；开启后缓冲区才会降到设定值，这条是有意的。

## [0.29.0-alpha] - 2026-09-02

### 修：「没有 U 盘就不录」这条规则只拦住了九分之一

正常模式下不往车机内置存储录 —— 这条规则原先只在**手动按录制按钮**那一条路上生效。
另外八处（开机自动录、悬浮按钮拉起、主题切换后恢复、定时自检、亮屏恢复）
都是直接开录的，没有 U 盘时会静悄悄地落到内置存储，只弹一句「已回退」。

也就是说，说好的「不写内置闪存」，实际上只在按按钮时成立。

判断搬进 `RecordingCoordinator.start()` —— 九条路现在是同一个答案。
自动录制会定时重试，同一条拒绝理由只提示一次，录起来或停下来时重置，
免得变成一串关不掉的提示。

开发者模式不受影响，仍可录到内置存储（手动路径上照旧先弹确认）。

### 首次启动引导改成四条

- 去掉了权限那条 —— 这台车机上相应权限是天然具备的，不必占一条；
- U 盘那条改成事实：**未检测到 U 盘时不会开始录制**（原先写的是「会提示并回退」，
  那是上面那个 bug 的表现，不是设计）；
- 去掉底部的致谢与开源声明 —— 它们的位置在「关于本应用」，首屏不重复。

### 英文界面（第三批，设置外的部分基本完成）

关于页、图片回看（含四份布局）、日期分组。中英各 297 条。

日期分组原先写死 `Locale.CHINESE` 和 `"MM月dd日"`，英文界面下会冒出一段中文日期。
改成让系统按当前语言拼格式：中文得到「8月28日」，英文得到「Aug 28」。
「今天 / 昨天 / 前天」走资源，而判断用的是**相对天数**而不是比对翻译过的文字 ——
比字符串的话，换一种语言就全对不上了。

### 顺带

- 图片分享的选项对话框没有指定 `AlertDialogTheme`，这个主题下按钮文字和背景同色，
  看起来就是「弹出来了但没有按钮」。项目里为此改过一轮，这是漏网的一个。
- 分享出去的文本里写着「来自 EVCam 的图片分享」—— 改名前留下的，
  发出去的标题写着另一个应用的名字。

### 仍是中文

诊断信息与 `AppLog` 日志，有意不翻译：它们是给开发者读的，
翻了只会让诊断报告和代码里的措辞对不上。补盲相关界面在开发者选项后面，同样不做。

## [0.28.0-alpha] - 2026-09-02

### 首次启动的引导换成了这个应用自己的内容

原来那一屏整段来自上游 EVCam：讲的是车型适配、自定义车型、录制模式，
还把上游作者写成「本软件作者」。在改名后的分支里，这既不适用，也不准确。

改成五条本应用真正需要先说清楚的：

1. 实验性非官方软件，未经车辆功能安全认证，不替代原厂设备，勿在行驶中操作；
2. 先授予相机、存储与悬浮窗权限；
3. 建议插入 U 盘 —— 未检测到时会提示并回退到内置存储，而持续写入消耗闪存寿命；
4. 超级后视镜默认关闭，以及它的三段手势；
5. 出问题先重启应用，仍不正常时导出诊断信息。

底部保留对 EVCam（GPL-3.0）的致谢，并写明免费开源、不出售。中英两份。

### 英文界面（第二批）

主录制界面的提示、前台服务通知、录制悬浮按钮、连续回放、检查更新、
侧边栏与抽屉头、三路布局的画面标签，全部走资源。中英各 268 条，一一对应。

### 删掉

五个远程指令处理方法（`handleStartRecordingCommand` 等）**没有任何调用方** ——
是钉钉远程控制那套功能拆掉后剩下的。它们的返回文案里还写着「EVCam」，
在这个分支里既够不着也不该出现。主界面 3888 → 3788 行。

### 还没做

关于页、诊断信息、图片回看的分享流程、日期分组（今天/昨天）仍是中文。
其中诊断信息是给开发者读的技术转储，**不打算翻译** —— 翻了反而会让
诊断报告和代码里的措辞对不上。

日志（`AppLog`）一律保持中文，同理。

## [0.27.0-alpha] - 2026-09-02

### 英文界面（第一批：设置）

设置界面的全部文案抽到了资源里，并配了一份英文。开发者选项不做英文 ——
它不面向普通用户。

**语言设置：设置 → 系统 → 语言**，三档：**跟随系统（默认）/ 中文 / English**。

默认跟随系统：车机是什么语言，应用就该是什么语言，这是不需要任何人做决定的默认。
另外两档留给「系统语言和使用者偏好不一致」的情况。

**首次启动时会先问一次语言**，预选「跟随系统」，直接关掉弹窗也停在这一档。
语言和「首次启动」两件事各记各的标记 —— 选完语言可能触发界面重建，
重建之后引导弹窗还得照常出现，共用一个标记会把引导吞掉。

切换**立即生效**，走的是 Android 自己的「按应用设定语言」
（`AppCompatDelegate.setApplicationLocales`），由系统重新加载资源并重建界面。
没有「重启后生效」的提示 —— 那种提示本身就意味着界面显示的和实际生效的暂时不是一回事。

Android 13 起，系统设置里的「应用语言」也能直接改本应用的语言。

### 顺带收紧了中文措辞

抽取文案时逐句过了一遍，去掉了偏口语的说法。例如
「窗口挪丢了或取景调乱了，用这个回到默认」改为
「窗口移出视野或取景调乱时，用此项恢复默认」。意思不变，只是更像说明书。

### 两条新的机械检查

- **中英条目必须一一对应，且占位符一致。** 少一条英文只是难看；
  而中文写了 `%1$s`、英文漏了，`getString(id, arg)` 在运行期直接抛异常，
  界面当场崩，编译期完全看不出来。
- 枚举选项的显示名改为优先取字符串资源，取不到才回落到代码里那份中文
  （分辨率、fps 这类本来也不需要翻译）。

### 已知范围

主界面、关于页、回放、悬浮窗提示这些还是中文，下一版处理。

## [0.26.1-alpha] - 2026-09-02

### 「检查更新」提到设置顶层

原来放在「关于」里面，那是最不容易被翻到的地方，而它是装新版本的唯一途径。
现在和各个分区并列，在「关于」上面一行。

### 新增 docs/rearview-gesture-spec.md

超级后视镜的完整操作规则与形状说明，全部数字取自代码而不是设想，
供绘制「首次开启时的操作说明图」使用。

## [0.26.0-alpha] - 2026-09-02

### 检查更新（重做）

**设置 → 关于 → 检查更新**：去 GitHub Releases 看有没有更新的版本，有就下载回来，
直接进系统的安装界面。

原先那一项指向的是上游作者的服务器，早先整理设置时删掉了。这次是重写的，
只认本仓库的 Releases。

**不用 `/releases/latest`**：那个接口只认正式版，会把预发布版全部跳过。
本项目发的全是 `-alpha`，用它会永远返回「没有更新」。所以拉列表，自己挑版本号最大的
那个非草稿版本。草稿版本对未登录的请求本来就不可见。

**版本号按段比，不按字符串比**：`"0.9.0" > "0.25.0"` 在字符串比较下成立，
那正好是「有更新却说没有」。tag 上的 `v` 前缀也要吃掉。认不出来的版本号一律当作
「没有更新」—— 误报会把人带去装一个来路不明的包，比漏报严重得多。这部分不碰网络，
有 8 条单元测试。

**关于「不落盘」**：系统装包必须从一个真实文件读，没法从内存直接装。能做到的是
不落到用户的存储里 —— APK 下到**应用缓存目录**（约 17 MB），
**每次检查更新前先把上一次的残留清掉**，空间紧张时系统也会自动回收这个目录。
下载中断或校验不过的残件会立刻删除：留着一个半截的 APK，下次只会得到一句
「解析包时出现问题」。

**这是本应用唯一一次主动出网**，只在点这一项时发生，去的是 `api.github.com`
和 GitHub 的下载域名，不带设备信息，也不上传任何东西。设置项的说明里写明了这一点。

新增 `REQUEST_INSTALL_PACKAGES` 权限。Android 8 起「安装未知来源应用」是一项单独授权，
没有它系统会静默挡掉安装界面 —— 这种情况会弹窗说明并带去授权页，
而不是让人对着一个没反应的按钮。

### 删掉

上游那套「更新服务器地址」配置（三个方法 + 指向 `evcam.suyunkai.top` 的默认地址）
一直没有任何调用方，删了。

## [0.25.2-alpha] - 2026-09-02

对「有没有功能因为丢了入口而被当成废弃代码删掉」做了一次全量核对，结论和修复如下。

### 核对方法

三条机械比对，不靠印象：

1. `AppConfig` 里 142 个 public setter，逐个查全应用有没有调用方 —— 没有调用方
   就等于这个设置再也改不了；
2. 旧那版设置界面（2850 行 Fragment + 1970 行布局）从 git 历史里取出来，
   把它改过的每一项和新界面比对；
3. 全部 Fragment / Activity，逐个查有没有地方能打开它。

### 修：自定义界面配置整个界面够不着了

`CustomCameraConfigFragment`（651 行）**没有任何入口**。而「视频流配置」里的
「自定义」在开发者选项打开后是选得出来的 —— 选了之后没有任何地方能配摄像头路数、
相机映射、旋转镜像和布局。

只有它能改的设置有 5 项：摄像头 id、摄像头名称、按钮样式、自由布局开关、屏幕方向。

入口加回开发者选项：**开发者选项 → 自定义界面配置**。

### 修：预览画面矫正的开关和调参入口都没了

早先那次「32 行设置压到 28 行」的整理里，我把「预览画面矫正」当作「在极氪上不起作用」删掉了。
**这个判断对合成流那一路成立，对别的不成立** —— 三路配置里的两路座舱相机和
自定义配置的相机走的是普通预览路径，`PreviewCorrection.postApply()` 对它们是生效的。

开关、调参悬浮窗、重置三项一并加回开发者选项（这两种配置本来也只在开发者模式里）。

### 没有加回来的，以及原因

- **鱼眼矫正**：它不是矩阵变换，而是在相机和 TextureView 之间插一层 GL
  （`FisheyeCorrector`）。这台车机上「用 GL 顶替相机的生产者会崩」是有记录的平台事实，
  而这个开关是**全局**的、没有分路开关 —— 一打开就会套到环视合成流那一路上。
  要安全地放出来，得先做到按路生效。**当前默认关，行为和现在一致。**
- **定制键唤醒**：靠 `VhalSignalObserver`，那个已经是空壳，开了也不会触发。
- **检查更新**：指向上游作者的服务器，本项目从 GitHub Releases 发布。
- 另有 10 项设置（副屏相机、车门联动、场景模式、新录制架构等）**从 EVCam 导入时
  上游就没有界面**，不是本项目删的。

### 结论

除上面列的之外，旧设置界面能改的每一项，新界面里都能改到。

## [0.25.1-alpha] - 2026-09-02

### 修：开发者选项里的条目点了全都没反应

接线的那段代码开头先找一个 key 为 `cat_developer` 的分类，找不到就整个返回。
而 0.21.0 把设置分区改成嵌套 `PreferenceScreen` 之后，**这个 key 就不存在了** ——
于是里面四个入口一个都没接上。

`findPreference` 找不到时返回 null，**不报错**，所以这块界面是安静地失效的：
点了没反应，日志里也没有任何线索。

补了一条单元测试：直接读 `preferences.xml` 和设置界面的源码比对，
代码里按 key 去拿的每一个 Preference，XML 里必须真有。不需要跑 Android。

### 修：解锁开发者模式后，要退出设置再进来才看得见

解锁是在「关于本应用」里做的，那是**另一个 Activity**。从那儿回来时，
左栏那份分区列表还是解锁之前建的，所以「开发者选项」不会出现。

现在回到设置时会检查一下解锁状态变没变，变了就重建列表。

### 补：权限工具（ADB 授权 / 系统白名单）的入口一直没接上

0.21.0 把权限设置改成 Preference 时，说明里写了「ADB 和白名单那几个工具
留在原来的界面里，从开发者选项进」—— 但**那一项从来没加进 XML**，
只有代码里一句接线，指向一个不存在的条目。

现在加上了：开发者选项 → 权限工具。

## [0.25.0-alpha] - 2026-09-02

### 修：设置二级界面按返回，会一路跳回录制界面

在「设置 → 权限设置」或「设置 → 相机映射」里按返回键，本该退回上一层设置分区，
实际会直接跳回录制界面。

0.21.0 把设置改成两栏之后，二级界面进的是**设置外壳自己的子返回栈**，
而返回键只查了活动这一层的返回栈 —— 那里是空的，于是判定为「不在二级菜单里」，
直接回录制界面。

现在返回键先问当前这一层能不能自己退一步。做法是把内容区的 Fragment 标成
Android 的**主导航 Fragment**，这本来就是框架用来认「当前是哪一层」的机制。

### 修：从二级界面直接切走，返回键会「按一次没反应」

补盲实验室、补盲校正这几个界面是压在返回栈上的。从那里用侧边栏切回录制界面时，
Fragment 被移走了，**返回栈却留着**。再按返回键，框架去反演一个已经不在屏幕上的
事务 —— 屏幕上什么都不变，得再按一次才退得出去。

回录制界面时把返回栈一并清掉。

### 切界面只留一份

「藏录制布局、亮出容器、replace、commit」原先在图片回看、设置、补盲三处各写一遍；
「先清所有选中再设当前项」在三处各写一遍。各收成一个方法。

回录制界面时移除 Fragment 也从「每个各提交一次事务」改成一个事务提完。

## [0.24.0-alpha] - 2026-09-02

### 修：自定义模式下相机可能一次都开不起来

开相机有一道闸门：等到「该有的画面都就绪了」才去初始化。这个「该有几路」
原先有两个来源 —— 先按布局的路数设一次，随后又被**按录制开关**算的数覆盖掉。

这两个是不同的问题。录制开关默认四路全开，而自定义模式下布局可能只给了
一两个 TextureView，于是闸门要等四路、最多只等得到两路，
`initCamera()` 一次都不会被调用 —— **全黑，连合成流都没有**。

这跟「环视+座舱3路」当初三路全黑是同一个坑，只是换了个来源。

现在这个数只有一个来源：布局给几路就是几路。按录制开关算的那个函数删掉了 ——
它回答的是另一个问题。

### 「有哪几路」这条规则只留一份

四个槽位依次是 front(1) / back(2) / left(3) / right(4)。上游只有 1、2、4 路的布局，
左右天然成对，于是好几处把 left 和 right 一起卡在 `>= 4` —— 在 1/2/4 路下看不出错。

「环视+座舱3路」是第一个 3 路配置，同一处笔误已经咬过两次：
一次是 texture_left 拿不到监听器（三路全黑），一次是录制集合里没有 left
（预览有画面、录出来少一路）。

抽成 `PreviewSlots`，挂预览监听和算录制集合都用它，补了 8 个单元测试。

### 删掉

`setCameraFrameVisible()` 没有任何调用方。

主界面 3868 → 3812 行。

## [0.23.0-alpha] - 2026-09-02

### 修：超级后视镜的开关会「拨上去但不出现」

没有悬浮窗权限时打开超级后视镜，开关会拨到「开」，还会弹一条提示告诉你手势怎么用 ——
而服务在 `onStartCommand` 里查了权限，直接 `stopSelf()` 走了，屏幕上什么都没有，
只往 logcat 写了一行。

而且它会一直「开」着：下次启动主界面时同样查权限、同样跳过，
开关却始终显示打开。

画面悬浮窗和录制悬浮按钮这两个开关都查了权限（查不到就去要授权、开关保持原位），
唯独后视镜这个漏了 —— 它是后加的，照着写的时候少抄了那一段。

现在三个开关走同一条路，权限判断只有一份。

### 悬浮窗的启停收到一个地方

「这个悬浮窗现在该不该开、能不能开」原先在三处各答一遍：主界面启动时、
设置页开关时、前台服务开机自启时。上面那个 bug 就是三份答案漂开的结果。

新增 `OverlayCoordinator`，负责画面悬浮窗 / 超级后视镜 / 录制悬浮按钮 / 补盲
四者的启动、开关、前后台切换和主界面销毁时的清理。

补盲的生命周期仍归 `BlindSpotService.update()` 自己管 —— 它按配置决定开哪几个窗、
关哪几个，协调器只回答「现在该不该捅它一下」。

### 补盲的启动条件两份合一（**这一条行为有变化**）

主界面判断补盲该不该起来，用的是「全局开关 且 至少一项子功能」或「定制键唤醒」。
前台服务在开机自启路径上自己又写了一遍，**条件比主界面窄**：只看四项子功能，
漏掉了全局开关、全景避让、定制键唤醒。

现在两处用同一个判断。开机自启路径因此有两处变化：

- 全局开关关着时，不再因为某个子开关还开着就把补盲拉起来（原来会）
- 全景避让或定制键唤醒开着时，现在会拉起来（原来不会）

这条规则有七个输入，光读代码看不出哪些组合会开，所以补了单元测试把它钉住。

### 其他

主界面 3915 → 3868 行。

## [0.22.0-alpha] - 2026-09-02

### 相机接线：两份抄的合成一份

主界面里「复用后台已有实例」和「新建实例」两条路，各自抄了一份完全相同的
8 个回调接线，91 行 × 2。逐行比过：除了一处注释和几个尾随空格，一模一样。

**已经开始漂，本身就是问题**。这种重复漂到功能上，表现是「同样的操作，
冷启动和从后台回来不一致」—— 两条路看着都对，这种最难查。

现在是一个 `wireCameraCallbacks()`，两处都调它。

### 删掉够不着的车型分支

`getCarModel()` 读出来就已经 sanitize 过 —— 设置里只列了「极氪7X」「极氪7X（环视+座舱3路）」
「自定义」三项，存的值不在这三项之内会被拨回极氪7X。也就是说这些分支**永远走不到**：

- 相机映射：银河 E5 / A7 / L6L7 / L7-多按钮 / 星舰7 / 手机模式（主界面和后台各一份）
- 预览变换：L7 的左右路旋转、手机模式的缩放
- 沉浸式状态栏：只对手机布局做的那一段
- 录制模式：L7 强制走 Codec

连同它们各自的映射方法一起删了。留着的坏处是读代码的人会以为它们还在用；
真要再支持别的车型，得先让它能被选出来。

**兜底的 else 一律保留** —— 它们走不走得到取决于运行期状态（比如合成流容器是不是 null），
不是读常量表能断定的。

### 初始化时不再打印相机清单

每次初始化都把所有相机的 `getCameraCharacteristics` 遍历一遍打出来，
而且尺寸列表截断到 5 条 —— 偏偏「HAL 到底有没有声明 1280×5140」这个唯一有人问的问题，
答案就在被截掉的那部分里。

同样的东西「诊断信息」页采得更全且不截断，极氪这一路 `ZeekrCameraLocator`
还会把候选尺寸和选用结果完整打一遍。所以这段是个更差的重复，删掉，只留一行相机数量。

### 行为没有变化

删的全是走不到的分支和重复的接线，能走到的路径一行没动。
主界面 4329 → 3915 行。

## [0.21.1-alpha] - 2026-09-01

### 录制的决策逻辑从主界面搬了出来

新增 `RecordingCoordinator`，负责「要不要录、能不能录、录起来」：
参数校验、时间戳、启动相机、前台服务、状态广播。

主界面只剩画面反馈：指示器、计时器、按钮、Toast。

**为什么分**：这两件事的正确性判据完全不同 —— 前者错了会丢录像，
后者错了只是看着别扭。混在一个方法里，想给前者加测试就得先有个 Activity。

**更实际的一条**：Android 会在主题切换、配置变更、内存吃紧时销毁并重建 Activity。
录制状态住在 Activity 里，就得靠一堆 `savedXxx` 变量在重建时接力
（代码里确实有这么三个）。这是往「录制不依赖某个窗口活着」走的第一步。

**行为没有任何变化** —— 所有 Toast、指示器、前台服务、悬浮窗广播都原样保留。

唯一删掉的是 L7-多按钮布局改按钮文字那两行：那个车型已经选不出来了。

### 这一步没搬什么

`isRecording` 这个字段还留在主界面（那里有四十多处在读它），通过回调保持同步。
把那些读取一并迁走是下一步，不该和这次混在一起 —— 一次改动只解决一个问题，
出了事才知道是哪一步的责任。

## [0.21.0-alpha] - 2026-09-01

### 设置改成两栏

左侧分区列表，右侧该分区的内容。车机内置屏 3200px 宽，
一列设置横铺过去右边两千多像素基本是空的，眼睛还得沿着很长的距离来回扫。

用的是 `SlidingPaneLayout` —— androidx 自己的 `PreferenceHeaderFragmentCompat`
内部也是它。宽屏并排、窄屏可滑动，两种情况都是白拿的。

**点子项时只换右栏**，左栏留着：进了二级界面还看得见自己在设置的哪一块。

分区名和顺序仍然只在 `preferences.xml` 里声明一次 —— 每个分区改成了嵌套的
`PreferenceScreen`，右栏按 key 取子树，左栏则把内容清空只留标题。
另写一份导航列表的话，加了新分区却忘了同步，左栏就会少一项。

### 权限设置改成 Preference

八项权限各一行，摘要直接显示授权状态；**已授权的那行不可点** ——
点了也只是又跳一次系统设置，给一个什么都不会发生的按钮不如不给。

回到这个界面时重新查一遍：授权是去系统设置里点的，回来时状态多半已经变了。

整个界面在代码里生成而不写 XML：每一项的状态是<b>查出来的</b>，不是存下来的设置值，
写死一份 XML 再逐个改标题摘要，等于把同一件事声明两遍。

ADB 一键授权和系统白名单那几个工具留在原来的界面里，从开发者选项进 ——
它们要往屏幕上滚动输出日志，那是控制台不是设置项。

### 删掉用不到的车型布局

`getCarModel()` 经 `SettingsRegistry` 净化，取值只可能是极氪两档或自定义。
布局选择器里那一长串银河 E5 / L6 / L7 / 星舰 / 手机分支判断的取值早就选不出来了。

删掉 10 个布局文件及其分支，连带清掉只存在于那些布局里的控件代码
（车辆控制按钮、前轮/后轮模式、E5 多按钮快捷键、四个摄像头开关）。

**净减约 4500 行。**

### 过渡动画用系统自带的

- 切分区：淡入淡出。横向滑动会读成「进入下一级」，而切分区是平级的
- 进二级界面：系统的「打开」过渡，返回时框架自动播反向动画

没有自定义动画曲线 —— 系统的这两个已经说明了层级关系，自己编一套只会和别处不一致。

## [0.20.0-alpha] - 2026-09-01

### 内置存储改为开发者模式专属

**正常模式下只录到外置存储**，车机内置闪存不再作为落盘位置。

理由是取舍，不是洁癖：行车记录是持续不断的写入，闪存写入寿命有限，
而车机存储坏了通常换不了。不熟悉软件的人会接受默认值、一路点确定 ——
这个代价不该由他们承担，所以它不该是一个弹窗能放行的选择。

具体：

- **存储位置**照样列出全部检测到的卷。「内置存储」仍然列出来（藏起来只会让人
  以为软件没这个能力），但标注**「仅限开发者模式」**，选中时会说明原因并拒绝。
- **中转写入**同样受限：它本质就是「先写内置再搬走」，也是往内置存储规律性写入。
- **没插 U 盘时不再偷偷降级**：以前会静默回退到内置存储继续录，
  现在会明确告诉你没检测到外置存储、不录，并提示插盘。

开发者选项打开后，以上限制全部解除。

### 关于页面：加了外置存储说明

推荐用法、已实测可用的介质，以及一条隐私提醒 ——
U 盘上的录像是不加密的，随时可以拔下来在别的设备上读。

### 超级后视镜启动延迟改为 2 秒

## [0.19.2-alpha] - 2026-09-01

### 修复：超级后视镜开着，但重开应用后不出现

开关状态存着「开」，可**没有任何地方在启动时把服务拉起来** ——
所以每次重开应用都得去设置里关一次再开一次，它才真的出现。

其他悬浮窗（画面悬浮窗、录制按钮、补盲）都在启动流程里恢复，唯独漏了它。现在补上了。

拉起来之前等了 1.5 秒：后视镜要绑相机，而那会儿相机还在开。

### 确认：超级后视镜默认是关的

查过了，`isRearViewEnabled()` 的默认值就是 `false`，不用改。

## [0.19.1-alpha] - 2026-08-30

### 修复：码率的「低/中/高」一直没生效

查证一个提问时发现的：这个下拉框**从来没有影响过录制**。

两处创建录制器的地方都写死了 `setQualityLevel(3)`（最高档），
而设置里的码率值只在「强制 H.264」时才会被读到 ——
正常的 HEVC 路径下走的是另一套公式，用的是那个写死的档位。

所以不管选低、中还是高，录出来都是同一个码率（约 7.7 Mbps）。

现在档位跟着设置走：

| 档位 | 码率 | 约合 |
|---|---|---|
| 低 | 3.8 Mbps | 28 MB/分钟 |
| 中 | 5.4 Mbps | 40 MB/分钟 |
| 高 | 7.7 Mbps | 58 MB/分钟 |

> ⚠️ **默认值的实际效果变了**：以前无论选什么都是 7.7 Mbps，
> 现在默认「中」是 5.4 Mbps —— 文件小了约三成，画质相应略降。
> 想要原来的画质，把码率调到「高」。

### 新增：录到内置存储前会提醒

手动开始录制时，如果录像实际会落在内置存储上，先弹一次确认。两种情况都算：

- 设置里选的就是内置存储
- 选了 U 盘但**当前没检测到**（会静默回退到内置，这种更值得提醒）

**自动录制不弹**（开机自启、回前台、远程唤醒）：那些场景没人在看屏幕，
一个等着点确定的弹窗会让记录仪根本不录 —— 那比写内置存储糟得多。
自动路径仍然用原有的 Toast 提示。

## [0.19.0-alpha] - 2026-08-30

### 修复：关掉鱼眼校正时，视野角度失效

那根滑块只喂给反投影，所以一关校正它就没反应了 ——
**一个拖得动却不起作用的控件，比没有这个控件更糟**。

现在它是超级后视镜的全局设置：开着校正时由反投影消化，关掉时由取景兑现
（收窄取景等效于放大），**四路都一样**。两种模式下拖动的手感一致。

换算和校正那条路同一套说法：180° 整幅，110° 约六成，90° 一半。
收窄不会改变画面比例 —— 比例锁死这条没有例外。

### 录像角标：加实时码率

规格行后面多了实时码率，**每秒更新一次**。

**不增加开销**：字节数在写进文件时本来就在手上，而角标位图本来就每秒重画一次
（秒数变了才重画）。只是把一个已有的数字接到一行已有的文字上。

显示的是**真正写进文件的**大小折算出来的，不是设置里的目标码率 ——
编码是可变码率，画面越复杂写得越多，目标值只是个上限。

### 录像角标：左上角标应用名与版本号

和右上角的日期时间对称。录出来的文件常常拿去当证据或者发给别人，
落上是哪个应用、哪个版本录的，回头出问题才对得上。

单独一块贴图而不是把右上角那块拉宽：两块在对角，一块横跨整幅会把字拉糊。
内容不变，初始化时画一次，之后不重画。

### 关于页面精简

原来来源说明和免责占了两屏，把这一页本该回答的问题挤没了。现在按
「这是什么 / 项目地址 / 请杯咖啡 / 开源与来源 / 安全须知」排，各一段。

- **项目地址**：github.com/dts88/zeekr-shortcut-car
- **请作者喝杯咖啡**：PayNow 到 9271 6786（完全自愿，不影响任何功能）

两处压缩了但没有删：GPL-3.0 的继承关系是许可证要求；
openavm-recorder 是「保留所有权利」的项目，「没有复制代码」这句必须留着。

## [0.18.0-alpha] - 2026-08-30

### 「分辨率与码率」并入设置

原来是一个单独的子界面（884 行 Java + 357 行布局）。现在直接是设置里的三项：
**录制分辨率**、**录制码率**、**视频流探测结果**。

分辨率选项要问过相机才知道，所以异步填；填好之前先显示当前值，不留空白下拉框。

**「亮度 / 降噪调节」**和它的**恢复默认**移到「高级」。

### 分辨率的取值规则单独提了出来

只列**每一路都支持**的尺寸，取交集而不是并集。

用并集的话，列表里会出现只有某一路才有的尺寸 —— 选中之后，
没有这个尺寸的那几路会悄悄退回最接近的一个，界面写的和实际录的就对不上了。
合成条带尺寸（1280×5140 之类）也排除：环视那一路固定用探测到的尺寸，这个设置管不到它。

这两条规则抽成了 `ResolutionOptions`，有单元测试钉着 ——
选错的表现是「录出来的分辨率和设置里写的不一样」，肉眼很难发现。

### 修正：录制界面的「前」不该改

上一版把「前」改成「环视」，改到了 `getDefaultCameraName`，
而**录制界面那四个标签也用同一个方法** —— 于是四宫格左上角也变成了「环视」，这是错的。
那里的四个标签指的是环视流拆出来的四个 lane，「前」就是前方。

改名挪到了 `getRecordingCameraDisplayName`：「录哪几路」和「四宫格里这一格叫什么」
是两个问题，答案也不一样。

### 删掉「使用说明」

那是上游 EVCam 的内容，讲的是这个应用里已经不存在的设置。
留着比没有更糟 —— 照着它调会调不出所写的效果。

## [0.17.1-alpha] - 2026-08-30

### 修复：内置存储选不了

确认框弹出来却没有确认键 —— 按钮其实在，只是文字和背景同色，看不见。

这个应用的主题下，原生 AlertDialog 必须显式带上 `AlertDialogTheme`
（项目里早有这个样式，注释就写着「确保文字可见」），我新加的那个框漏了。

### 存储位置：列出真实存在的卷

以前只有「U盘 / 内置」两项，插两个盘时没法指定是哪一个。

现在按实际探测结果逐个列出，每一项带**卷名 + 剩余/总容量**：

```
U盘 1 (A1B2-C3D4)（剩余 45.2 GB / 64.0 GB）
U盘 2 (E5F6-7890)（剩余 12.1 GB / 32.0 GB）
内置存储
```

选中某个盘会把它的根目录钉住，否则检测逻辑永远落到第一个盘上。

### 极氪车型下「前」改为「环视」

那一路不是前摄像头，是车机拼好的整条 360° 环视流。
叫「前」会让人以为还有另外三路可选。多路配置下另外两路相应叫「座舱1」「座舱2」。

### 新增：开发者选项

在**「关于本应用」里连点「安全须知」20 次**，输入密码后打开。

以下移入其中，平时不显示：

| 项目 | 为什么 |
|---|---|
| 视频流配置的「环视+座舱3路」「自定义」 | 没做完 |
| 权限设置 | 排查用 |
| 上传日志 | 排查用 |
| 补盲选项 | 未完成 |
| 超视模式 | 未完成 |

**不持久化**：只存在内存里，所以应用更新后失效、冷启动后失效
（从后台切回前台不算，那时进程没变）。用完不用记得关，重启自然就没了。

半成品混在正常选项里，选中之后出问题会让人以为是应用坏了 —— 所以默认藏起来。

> 当前值一定保留：万一已经停在某个隐藏选项上，把它从列表里抹掉会让下拉框空白。

## [0.17.0-alpha] - 2026-08-30

### 设置界面重建在 PreferenceScreen 上

原来是 **1970 行手写布局 + 2850 行接线代码**。每加一个设置都要重复写一遍
「卡片 + 标题 + 说明 + 控件 + 找 id + 读值 + 写值 + 联动显隐」，
而分类、摘要、启用依赖这些框架本来就提供。

现在是 **330 行 XML + 640 行接线**，净减约 3800 行。

### 最实在的收益：启用依赖不再靠手写

以前「息屏录制要先开启动自动录制」这类关系，是靠手写显隐逻辑维持的，
写漏一处就会出现「开关能点但不生效」。现在用 `android:dependency` 声明，框架负责置灰：

- 息屏录制 ← 启动自动录制
- 角标附带录制规格 ← 时间角标
- 后视镜的四项设置 ← 超级后视镜开关
- 悬浮窗大小/透明度 ← 悬浮窗开关
- 按钮大小/文字大小 ← 录制悬浮按钮
- 预览分辨率 ← 预览用低分辨率

### 取值仍然一律走 AppConfig

所有 Preference 都 `setPersistent(false)`：**它们不自己往 SharedPreferences 里写**，
读写全部经过 AppConfig。

这一条是刻意的。AppConfig 的 getter/setter 里带着夹取、默认值和联动，
而且它是这些设置在整个应用里唯一的读取入口。让 Preference 自己持久化就多了一条写入路径，
key 稍有出入就是「设置看着改了、实际没生效」—— 这类问题在这个项目里已经栽过几次。
所以 XML 里的 key 只是标识符，不是存储键。

**因此升级不会丢设置**：存的还是同一份 `app_config`，同样的键和值。

### 分类

返回录制界面 / 录制 / 存储 / 超级后视镜 / 悬浮窗 / 系统 / 高级 / 关于

「返回录制界面」单独放在最上面一条 —— 车机上没有系统返回键，
侧滑抽屉行车时也不好按。

### 搬运过程中修掉的两处

- **滑块范围是编的**：我一开始给悬浮窗大小写了 100–600，
  实际是 `FLOATING_SIZE_TINY`(32)–`FLOATING_SIZE_MAX`(160)；
  按钮大小实际 32–100，文字 8–24。现在直接引用 AppConfig 的常量。
  范围写错的表现是「选了一个随后被夹掉的值」，又是显示与实际不符那一类。
- **漏了实时推送**：改悬浮窗大小/透明度要通知正在显示的窗口，
  否则得关掉再开才看得到。

### 录制摄像头改成多选

用 `MultiSelectListPreference`，选项按**这台车实际有几路**生成，
不再固定列四个。至少要保留一路 —— 全不勾等于关掉录制，而关录制有它自己的开关。

### 几个弹窗是原样搬的

相机映射、使用说明、日志上传（含设备名输入与确认）这几段流程
从旧 Fragment 里**整段搬到了 `SettingsDialogs`**，只把 `getContext()` 和 `appConfig`
换成参数。没有重写：这些流程里的细节（覆盖项的清除时机、上次会话日志的判断）
重推一遍容易漏。

日志上传原来会把按钮置灰改文字作为「上传中」的反馈；
PreferenceScreen 里点完对话框就关了，没有那个按钮，改成用 Toast 报开始和结果。

### ⚠️ 这是目前改动面最大的一版

设置界面每一项都重新接了一遍线，而我没法在车上验证。
**请把每个设置都点一遍**，尤其是开关类的（看看改完是否真的生效），
以及录制摄像头、存储位置、相机映射、日志上传这几个带弹窗的。

设置值本身不会丢 —— 存储格式没变，真出问题回退到 0.16.0 也读得到。
