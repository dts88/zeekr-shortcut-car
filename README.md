# Zeekr Shortcut (Car Version)

面向极氪车机的环视记录仪应用。

[English](README.en.md) · [来源与致谢](NOTICE.md) · [更新日志](CHANGELOG.md)

> [!WARNING]
> 本项目是**实验性的非官方软件**，与极氪（ZEEKR）没有任何关联，未经其批准或认可，
> 也未经过任何车辆功能安全认证。它**不能替代**原厂行车记录仪、倒车影像、盲区监测
> 或任何法定安全设备。请勿在行驶中操作，请勿依据本应用的画面判断车距或障碍物。
> 使用风险由使用者自行承担。详见文末「安全须知」。

---

## 这个项目是什么

极氪 App Lab 环境下，第三方应用拿不到四个独立的环视摄像头，只能拿到**一路已经拼接好的
合成视频流**——四个 1280×1280 的方形画面挤在同一帧里。直接预览就是一根又细又长的竖条，
比例完全不对，也没法当记录仪用。

本应用做两件事：

1. **把合成流正确地拆开显示**——按真实几何切成四宫格，保持每个画面原本的方形比例；
2. **接上一套成熟的记录仪功能**——多路视频流接入、H.264/H.265 编码、码率与帧率调节、
   U盘等外置存储、分段录制、自动清理、回放、悬浮窗、远程查看。

这两件事**都不是从零写的**，分别建立在两个已有项目之上。下面说清楚来源。

---

## 功能来源与致谢

本应用的能力来自两个项目。没有它们，这个应用不会存在。**在此向两位原作者致以诚挚的感谢。**

### 来源一：EVCam —— 代码基座

**项目**：<https://github.com/suyunkai/EVCam>
**作者**：suyunkai
**许可证**：GNU GPL v3.0

本仓库**直接以 EVCam 的源代码为基座**。仓库的第一个 commit 就是 EVCam 在
`0876b97` 时的完整工作树，此后所有改动都能以 diff 的形式看清楚。

以下功能**全部来自 EVCam**，著作权归其作者所有：

| 能力 | 说明 |
|------|------|
| 多路视频流接入 | Camera2 管线、多摄像头会话管理、Surface 共享 |
| 视频压缩 | MediaRecorder 硬编码 / OpenGL + MediaCodec 软编码，H.264 与 H.265 可选 |
| 码率调节 | 高/中/低档位，按像素与帧率自适应计算 |
| 帧率调节 | 标准/低帧率，运行时可降帧 |
| 编码缩放 | 100% / 75% / 50% / 25% |
| 外置存储 | U盘检测、公共目录写入、中转写入、掉盘回退 |
| 存储管理 | 容量上限、旧文件自动清理 |
| 分段录制 | 1 / 3 / 5 分钟自动分段 |
| 时间戳水印 | 多种样式与颜色 |
| 回放 | 视频与图片分组回看、缩略图缓存 |
| 悬浮窗 | 可调大小与透明度的快捷入口 |
| 保活 | 开机自启、前台服务、WorkManager、无障碍服务 |
| 远程查看 | 钉钉 / 飞书 / Telegram 机器人 |

按照 GPL v3.0 的要求，**本应用同样以 GPL v3.0 发布，并公开全部源代码**。

### 来源二：AVM Recorder（openavm-recorder）—— 极氪合成流的公开技术资料

**项目**：<https://github.com/Dantenothing/openavm-recorder>
**作者**：Dantenothing
**许可**：`Copyright © 2026 Dantenothing. All rights reserved.`（**保留所有权利，非开源许可**）

「极氪 App Lab 只给一路合成流」这个关键事实，以及合成流的具体尺寸与排布，
是由 openavm-recorder 项目率先公开记录的。其公开 README 中记载：

- **竖排 `1280×5140`**：四个 1280×1280 方形画面竖向堆叠，
  `5140 = 4×1280 + 20`，多出的 20 行是 5 条 4px 分隔带（上边缘、三条内部分隔、下边缘）；
- **横排 `5120×1280`**：四个 1280×1280 画面横向排列，`5120 = 4×1280` 正好整除；
- 整条合成流的推荐编码码率约 **28 Mbps**。

本应用对合成流的**界面比例与四宫格拆分**，正是基于以上公开记录的技术事实实现的。
没有这份公开资料，本应用不可能适配极氪车机。**特此致谢。**

> [!IMPORTANT]
> **本项目没有复制 openavm-recorder 的任何源代码。**
>
> 该项目保留所有权利，明确声明未授予开源许可，其源代码与 APK 不可被复制、修改或再分发。
> 本项目只使用了其公开 README 中记载的**接口事实**（分辨率、排布、分隔带、比例、码率），
> 这类对硬件输出接口的事实性描述不构成受保护的表达。
> 四宫格拆分（[`CompositeStreamGeometry`](app/src/main/java/com/kooo/evcam/zeekr/CompositeStreamGeometry.java)）
> 与 GL 渲染（[`CompositeTextureView`](app/src/main/java/com/kooo/evcam/zeekr/CompositeTextureView.java)）
> 均为独立编写实现。
>
> 本项目与 openavm-recorder 及其作者**没有隶属或合作关系，也未获其背书**。
> AVM Recorder 的名称与图标未用于本应用的品牌标识。

完整的来源、许可证与边界说明见 [NOTICE.md](NOTICE.md)，应用内「关于与致谢」页面同样完整展示。

---

## 极氪合成流是怎么处理的

```
Camera2（车机唯一的一路合成流，如 1280×5140）
        │  写入
        ▼
生产者 SurfaceTexture（OES 外部纹理）
        │  GL 线程按几何拆成 4 个采样窗口
        ▼
2×2 四宫格 ── 渲染到 TextureView 的显示 Surface
```

关键实现点：

- **不改上游相机管线**。`CompositeTextureView` 继承 EVCam 的 `AutoFitTextureView`，
  只是重写 `getSurfaceTexture()` 返回自己的 OES 纹理。上游 `SingleCamera` 一行都不用改。
- **按能力找相机，不写死下标**。`ZeekrCameraLocator` 遍历所有 Camera2 设备，
  谁声明了合成流尺寸就用谁。
- **绝不臆造分辨率**。只从 HAL 实际声明的尺寸中挑选；一个都没有就明确提示
  「本机不支持」，而不是拿猜的尺寸去开相机换来黑屏。
- **比例优先**。默认 `FIT` 模式：保持每个画面原本的方形比例，格子里留黑边而不是拉伸。
- **未知排布安全回退**。分隔带对不上时退回四等分，宁可略有偏移也不裁掉真实画面。

录制保存的是**原始合成流**（不做拆分再编码），这样编码开销最低、也不损失画质；
回放时用同一套几何还原成四宫格。

### 交互

- 点击右侧「四宫格 / 单画面」按钮切换显示模式
- 单画面模式下点击画面，循环切换到下一路
- 画面底部会显示识别结果（尺寸、分隔带、拆分方式），方便确认与排查

---

## 安装

### 从 Releases 下载（推荐）

前往 [Releases](../../releases) 下载最新的 `ZeekrShortcut-*.apk`，通过 App Lab 侧载安装。

每次推送到 `main` 分支，GitHub Actions 也会自动构建一份 APK，
可在 [Actions](../../actions) 页面的构建产物中下载。

### 自行构建

需要 JDK 17+ 与 Android SDK（compileSdk 36）：

```bash
git clone https://github.com/dts88/zeekr-shortcut-car.git
```

```bash
cd zeekr-shortcut-car && ./gradlew assembleRelease
```

产物位于 `app/build/outputs/apk/release/`。

仓库内含一个**公开的测试签名密钥**（AOSP 测试签名，口令 `android`），
目的是让任何人都能构建出可覆盖安装的 APK。正式发布请用自己的密钥，
通过环境变量覆盖：`ZEEKR_KEYSTORE`、`ZEEKR_KEYSTORE_PASSWORD`、`ZEEKR_KEY_ALIAS`、`ZEEKR_KEY_PASSWORD`。

---

## 使用

1. 安装后打开应用，进入 **菜单 → 软件设置 → 车型**；
2. 选择 **「极氪7X（环视合成流）」**，按提示重启应用；
3. 回到录制界面，应当看到四宫格环视画面；
4. 若提示「未检测到四联合成流」，说明本机 Camera2 没有声明合成流尺寸，
   该车机/固件版本可能不适用。

其余录制、存储、编码设置沿用 EVCam 原有的设置页，位置未变。

---

## 已知限制

- **只在极氪 7X 的合成流格式上做过设计**。其他极氪车型或固件版本若排布不同，
  会走等分回退，画面可能有偏移。
- **未在实车上验证**。本仓库的自动化测试只覆盖纯逻辑部分（几何拆分、相机选择）；
  GL 渲染、相机取流、录制链路需要实车验证。**首次使用请在静止车辆上测试。**
- **合成流分辨率极高**（约 655 万像素），编码压力大。默认帧率 20fps；
  如遇卡顿或掉帧，请调低码率档位或使用编码缩放。
- **EVCam 的部分功能面向吉利银河车型**（VHAL 信号、补盲、转向灯联动等），
  在极氪车机上大概率不可用，但不影响记录仪主功能。
- **APK 体积较大**。App Lab 提示过体积过大的应用可能无法启动；若遇到，请提 issue。
- 长时间录制会持续写入闪存，请自行评估存储损耗。

---

## 安全须知

- 本应用为实验性非官方软件，未经任何车辆功能安全认证；
- 不可替代原厂行车记录仪、倒车影像、盲区监测或任何法定安全设备；
- **请勿在行驶中操作本应用**，请勿依据其画面判断车距、障碍物或进行变道/倒车；
- 画面存在延迟，可能卡顿、撕裂、失真或中断；
- 与车机系统争夺资源**可能**影响原厂功能，一旦出现异常请立即停止使用并卸载；
- 录制内容可能包含人脸、车牌、住址、行程等个人信息，
  请遵守所在地关于隐私、监控、录制与传播的法律，未经同意不要公开可识别的影像；
- 本软件以「现状」（AS IS）提供，不提供任何形式的明示或默示担保。
  因使用或安装本软件导致的任何人身伤害、财产损失或交通事故，开发者不承担法律责任。

---

## 许可证

**GNU General Public License v3.0** —— 继承自 EVCam。

你可以自由使用、修改和分发本项目，但对外分发时必须同样以 GPL-3.0 发布并提供对应源代码，
保留版权与许可声明，且不得添加额外限制。完整条款见 [LICENSE](LICENSE)。

第三方组件与来源项目的许可情况见 [NOTICE.md](NOTICE.md)。
