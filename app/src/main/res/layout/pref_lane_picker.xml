<?xml version="1.0" encoding="utf-8"?>
<!--
  摆位舞台：每一格按它真实的位置和大小画出来，点一格选它，拖一格换位置，
  拖四个角改大小。改的是哪一格、改成了什么样，都在这张图上。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:background="@drawable/bg_pref_row_static"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@android:id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_secondary"
        android:textAppearance="@style/TextAppearance.Cam.Micro" />

    <com.kooo.evcam.settings.LaneMapView
        android:id="@+id/lane_map"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp" />

    <TextView
        android:id="@android:id/summary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone" />
</LinearLayout>
